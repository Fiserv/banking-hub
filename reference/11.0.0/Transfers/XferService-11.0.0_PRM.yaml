---
openapi: 3.0.1
info:
  title: Transfer Service
  description: The Transfer service supports operations related to managing the transfer of money between accounts. The service includes the description of the FROM and TO accounts, amount of the transfer, transfer fees and frequency of the transfer as it can be a one-time transfer or a recurring transfer.
  version: 11.0.0_PRM
servers:
- url: /
paths:
  /xferservice/payments/transfers:
    put:
      tags:
      - Update Transfer
      summary: Update Transfer
      description: "Update Transfer API modifies the information on an existing scheduled transfer (one time or recurring). This API allows modifications of the TO account, the amount, date and the frequency of the transfer. <br> <br> To download the API Specifications and Postman Collection, click <a href=\"../docs/?path=docs/resources/resources.md\" title=\"Click to open\">Resources</a>."
      operationId: XferModOper
      parameters:
      - name: EFXHeader
        in: header
        description: EFX Header is a common and required component for all API calls, it contains client-related parameters. Please note that the header parameters should be sent as a JSON object under the EFXHeader parameter. To view the list of header parameters, <a href="../docs?path=docs/api-ref-EFX-header.md" title="Click to open"> click here.</a>
        required: true
        schema:
          $ref: '#/components/schemas/EFXHeader'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/XferModOperRequest'
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/XferModOperResponse'
        default:
          description: To view the complete list of response codes, <a href="../docs/?path=docs/response-codes.md" title="Click to open">click here.</a>
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/XferModOperResponse'
      x-default-example: Premier
      x-group-name: Transfer Service
      x-proxy-name: Update Transfer
      x-child-product-name: Transfers
      x-core: Premier
    post:
      tags:
      - Add Transfer
      summary: Add Transfer
      description: "Add Transfer API creates the request for an automatic transfer of funds between two accounts. <br> For Premier, this API supports future dated transfers on the core and includes the FROM and TO accounts, descriptions and the frequency of the transfer which can be a one-time transfer or a recurring transfer. <br> <br> To download the API Specifications and Postman Collection, click <a href=\"../docs/?path=docs/resources/resources.md\" title=\"Click to open\">Resources</a>."
      operationId: XferAddOper
      parameters:
      - name: EFXHeader
        in: header
        description: EFX Header is a common and required component for all API calls, it contains client-related parameters. Please note that the header parameters should be sent as a JSON object under the EFXHeader parameter. To view the list of header parameters, <a href="../docs?path=docs/api-ref-EFX-header.md" title="Click to open"> click here.</a>
        required: true
        schema:
          $ref: '#/components/schemas/EFXHeader'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/XferAddOperRequest'
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/XferAddOperResponse'
        default:
          description: To view the complete list of response codes, <a href="../docs/?path=docs/response-codes.md" title="Click to open">click here.</a>
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/XferAddOperResponse'
      x-default-example: Premier
      x-group-name: Transfer Service
      x-proxy-name: Add Transfer
      x-child-product-name: Transfers
      x-core: Premier
  /xferservice/payments/transfers/secured:
    put:
      tags:
      - Delete Transfer
      summary: Delete Transfer
      description: "Delete Transfer API deletes the existing scheduled (one time or recurring) transfer of funds from the core. <br> <br> To download the API Specifications and Postman Collection, click <a href=\"../docs/?path=docs/resources/resources.md\" title=\"Click to open\">Resources</a>."
      operationId: SecuredXferDelOper
      parameters:
      - name: EFXHeader
        in: header
        description: EFX Header is a common and required component for all API calls, it contains client-related parameters. Please note that the header parameters should be sent as a JSON object under the EFXHeader parameter. To view the list of header parameters, <a href="../docs?path=docs/api-ref-EFX-header.md" title="Click to open"> click here.</a>
        required: true
        schema:
          $ref: '#/components/schemas/EFXHeader'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SecuredXferDelOperRequest'
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SecuredXferDelOperResponse'
        default:
          description: To view the complete list of response codes, <a href="../docs/?path=docs/response-codes.md" title="Click to open">click here.</a>
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SecuredXferDelOperResponse'
      x-default-example: Premier
      x-group-name: Transfer Service
      x-proxy-name: Delete Transfer
      x-child-product-name: Transfers
      x-core: Premier
    post:
      tags:
      - Get Transfer
      summary: Get Transfer
      description: "Get Transfer API retrieves the information about one specific scheduled transfer (one time or recurring) from the core, based on the unique transfer identifier sent in the request. This API returns the TO and FROM accounts details, the amount, start date and end date and the frequency of the transfer and other details in the response. <br> <br> To download the API Specifications and Postman Collection, click <a href=\"../docs/?path=docs/resources/resources.md\" title=\"Click to open\">Resources</a>."
      operationId: SecuredXferInqOper
      parameters:
      - name: EFXHeader
        in: header
        description: EFX Header is a common and required component for all API calls, it contains client-related parameters. Please note that the header parameters should be sent as a JSON object under the EFXHeader parameter. To view the list of header parameters, <a href="../docs?path=docs/api-ref-EFX-header.md" title="Click to open"> click here.</a>
        required: true
        schema:
          $ref: '#/components/schemas/EFXHeader'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SecuredXferInqOperRequest'
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SecuredXferInqOperResponse'
        default:
          description: To view the complete list of response codes, <a href="../docs/?path=docs/response-codes.md" title="Click to open">click here.</a>
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SecuredXferInqOperResponse'
      x-default-example: Premier
      x-group-name: Transfer Service
      x-proxy-name: Get Transfer
      x-child-product-name: Transfers
      x-core: Premier
  /xferservice/payments/transferlist/secured:
    post:
      tags:
      - Get Transfer List
      summary: Get Transfer List
      description: "Get Transfer List API retrieves a list of scheduled transfers (one time or recurring) from the core. This API returns the TO and FROM accounts details, the amount, date and the frequency of the transfer and other details in the response. <br> <br> To download the API Specifications and Postman Collection, click <a href=\"../docs/?path=docs/resources/resources.md\" title=\"Click to open\">Resources</a>."
      operationId: XferListInq
      parameters:
      - name: EFXHeader
        in: header
        description: EFX Header is a common and required component for all API calls, it contains client-related parameters. Please note that the header parameters should be sent as a JSON object under the EFXHeader parameter. To view the list of header parameters, <a href="../docs?path=docs/api-ref-EFX-header.md" title="Click to open"> click here.</a>
        required: true
        schema:
          $ref: '#/components/schemas/EFXHeader'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/XferListInqRequest'
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/XferListInqResponse'
        default:
          description: To view the complete list of response codes, <a href="../docs/?path=docs/response-codes.md" title="Click to open">click here.</a>
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/XferListInqResponse'
      x-default-example: Premier
      x-group-name: Transfer Service
      x-proxy-name: Get Transfer List
      x-child-product-name: Transfers
      x-core: Premier
components:
  schemas:
    XferAddOperRequest:
      required:
      - XferInfo
      properties:
        OvrdExceptionData:
          $ref: '#/components/schemas/OvrdExceptionDataType'
        XferInfo:
          $ref: '#/components/schemas/XferAddOperRequest_XferInfo'
    XferAddOperResponse:
      required:
      - XferStatusRec
      properties:
        Status:
          $ref: '#/components/schemas/StatusType'
        XferStatusRec:
          $ref: '#/components/schemas/XferAddOperResponse_XferStatusRec'
    SecuredXferDelOperRequest:
      required:
      - XferKeys
      properties:
        XferKeys:
          $ref: '#/components/schemas/SecuredXferDelOperRequest_XferKeys'
    SecuredXferDelOperResponse:
      required:
      - XferStatusRec
      properties:
        Status:
          $ref: '#/components/schemas/StatusType'
        XferStatusRec:
          $ref: '#/components/schemas/SecuredXferDelOperResponse_XferStatusRec'
    SecuredXferInqOperRequest:
      required:
      - XferSel
      properties:
        XferSel:
          $ref: '#/components/schemas/SecuredXferInqOperRequest_XferSel'
    SecuredXferInqOperResponse:
      required:
      - XferRec
      properties:
        Status:
          $ref: '#/components/schemas/StatusType'
        XferRec:
          type: array
          description: Details of the transfer record returned in the response for the specified search criteria.
          items:
            $ref: '#/components/schemas/SecuredXferInqOperResponse_XferRec'
    XferListInqRequest:
      required:
      - XferListSel
      properties:
        XferListSel:
          $ref: '#/components/schemas/XferListInqRequest_XferListSel'
    XferListInqResponse:
      required:
      - XferListRec
      properties:
        Status:
          $ref: '#/components/schemas/StatusType'
        RecCtrlOut:
          $ref: '#/components/schemas/XferListInqResponse_RecCtrlOut'
        XferListRec:
          type: array
          description: Details of the transfer list record returned in the response for the specified search criteria.
          items:
            $ref: '#/components/schemas/XferListInqResponse_XferListRec'
    XferModOperRequest:
      required:
      - XferInfo
      - XferKeys
      properties:
        OvrdExceptionData:
          $ref: '#/components/schemas/OvrdExceptionDataType'
        XferKeys:
          $ref: '#/components/schemas/XferModOperRequest_XferKeys'
        XferInfo:
          $ref: '#/components/schemas/XferModOperRequest_XferInfo'
    XferModOperResponse:
      required:
      - XferStatusRec
      properties:
        Status:
          $ref: '#/components/schemas/StatusType'
        XferStatusRec:
          $ref: '#/components/schemas/XferModOperResponse_XferStatusRec'
    EFXHeader:
      required:
      - OrganizationId
      type: string
      properties:
        OrganizationId:
          maxLength: 36
          type: string
          description: Organization Identifier is a unique identifier that represents the financial institution or holding company.  This identifier does not necessarily match the organization identifier or bank identifier in the backend system. To ensure uniqueness across all institutions in all parts of the world, use the Dun & Bradstreet number.
        TrnId:
          maxLength: 36
          type: string
          description: Transaction UUID (Universal Unique Identifier) of the current message.
        VendorId:
          maxLength: 255
          type: string
          description: Unique vendor identification number provided by Fiserv to identify the vendor.
        Context:
          $ref: '#/components/schemas/Context'
      example: '{ "TrnId": "622182", "OrganizationId": "PRMOrg10", "VendorId": "112233", "Context":{ "Channel": "WEB" } }'
    Context:
      type: object
      properties:
        ClientAppName:
          maxLength: 40
          type: string
          description: Name of the client application that is used to send the service request, such as Architect, Commercial Center and others.
        Channel:
          maxLength: 80
          type: string
          description: Name of the channel used by the client application. * 'Online' * 'Phone'  * 'Branch' * 'EFT'
        TrnIdent:
          maxLength: 36
          type: string
          description: Unique identification value of transaction defined by the client.  Also known as Trace Number or Sequence in some systems. When used by a Network defined transaction,  the value of this identifier is typically set to narrow character of 12.
        ClientDateTime:
          pattern: '[0-9]{4}-[0-9]{2}-[0-9]{2}(T[0-9]{2}:[0-9]{2}(:[0-9]{2}(\.[0-9]{3})?((-|\+)[0-9]{2}:[0-9]{2})?)?)?'
          type: string
          description: Client date and time as reported by the client application that is making the service request.
        ClientTerminalSeqNum:
          maxLength: 36
          description: 'Client terminal sequence number. type: string'
        OriginatorType:
          type: integer
          description: "The type of organization that the originator represents. This is usually a coded value representing the industry that the organization operates in. It contains the SIC code. For ISO 8583 (DE18) the element is restricted to narrow character of maximum length of 4.  Originator Type codes that apply to financialinstitutions are:\n  6010 - Financial institution—bank, savings and loan (S and L), or credit union  \n  6011 - Financial institution—ATM \n  6012 - Financial institution—merchandise sale \n  6999 - Financial institution—home banking"
          format: int64
        TerminalIdent:
          maxLength: 36
          type: string
          description: Unique code identifying a terminal at the card acceptor location (such as terminal code or terminal number of ATM). See For ISO 8583 (DE41) the element is restricted to a narrow character with maximum length of 8.
        TerminalAddr1:
          maxLength: 64
          type: string
          description: Terminal address line 1.
        TerminalAddr2:
          maxLength: 64
          type: string
          description: Terminal address line 2.
        TerminalAddr3:
          maxLength: 64
          type: string
          description: Terminal address line 3.
        TerminalAddr4:
          maxLength: 64
          type: string
          description: Terminal address line 4.
        TerminalCity:
          maxLength: 32
          type: string
          description: Terminal city name.
        TerminalCounty:
          maxLength: 32
          type: string
          description: Terminal county name.
        TerminalStateProv:
          maxLength: 32
          type: string
          description: State province value as per ISO 3166-2:US codes where the terminal is located.
        TerminalPostalCode:
          maxLength: 11
          type: string
          description: 'Postal Code where the terminal is located. '
        TerminalCountryCodeSource:
          maxLength: 80
          type: string
          description: |-
            Standard country code format where the terminal is located. Valid values are:
              * 'ISO3166-Numeric'
              * 'ISO3166-Alpha-3'
        TerminalCountryCodeValue:
          maxLength: 20
          type: string
          description: Country code value as per the ISO source code set in the TerminalCountryCodeSource field.
        PhoneType:
          maxLength: 80
          type: string
          description: 'Type of a phone. Valid values are: * ''EvePhone'' * ''DayPhone'' * ''EveFax'' * ''DayFax'' * ''Home'' * ''Work'' * ''Mobile'' * ''Fax'' * ''Pager'' * ''Modem'' * ''Other'''
        PhoneNum:
          type: string
          description: Phone number.
        PhoneExchange:
          maxLength: 80
          type: string
          description: Phone exchange.
        TerminalOwnerName:
          maxLength: 40
          type: string
          description: Name of the owner or operator of the terminal.  For ISO 8583 (DE43) the element is restricted to C-15.
        SystTraceAuditNum:
          maxLength: 36
          type: string
          description: Number assigned by the transaction originator to assist in identifying a transaction uniquely. The trace number remains unchanged for all messages throughout the life of a transaction. This number is not a terminal receipt number. The originating processor increments the trace number by one for each transaction sent to the switch. For ISO 8583 (DE11) the element is restricted to NC-6
        NetworkIdent:
          maxLength: 80
          type: string
          description: Identifies the interchange network for the transaction. The transaction is applied to the specified network settlement counts and balances. For ISO 8583 (DE62) the element is restricted to NC-6
        NetworkRefIdent:
          maxLength: 36
          type: string
          description: Document reference supplied by the system retaining the original source document and used to assist in locating that document. The acquirer of a transaction assigns this number. The issuer processor must retain it in the event that a chargeback is submitted for the transaction. For ISO 8583 (DE37) the element is restricted to C-12
        AcquirerIdent:
          maxLength: 36
          type: string
          description: Identifier of the acquirer who processes the financial transaction. It is a mandatory element in all authorization and financial messages and does not change throughout the life of a transaction. EPOC considers the acquirer as the terminal owner for reporting purposes. For ISO 8583 (DE32) the element is restricted to C-12
        AcquirerCountryCodeSource:
          maxLength: 80
          type: string
          description: |-
            Standard country code format where the acquirer is located. Valid values are:
              * 'ISO3166-Numeric'
              * 'ISO3166-Alpha-3'
        AcquirerCountryCodeValue:
          maxLength: 20
          type: string
          description: Country code value of Acquirer as per the ISO source code set in the AcquirerCountryCodeSource field.
        MerchNum:
          maxLength: 23
          type: string
          description: Unique identification number of a merchant.  For ISO 8583 (DE42) the element is restricted to C-15
        SettlementDate:
          type: string
          description: Transaction settlement date. Used by ISO 8583 (DE63).
          format: date
        SettlementIdent:
          maxLength: 36
          type: string
          description: Identification of the settlement account in which the transaction will be settled.
        BusinessApplIdent:
          maxLength: 80
          type: string
          description: |-
            Identification of the business application. Valid values are:
              * 'P2P' - Person-to-Person
              * 'C2B' - Consumer-to-Business
              * 'A2A' - Account-to-Account
              * 'B2C' - Business-to-Consumer
              * 'B2B' - Business-to-Business
              * 'G2C' - Government-to-Consumer
              * 'C2G' - Consumer-to-Government
        BranchIdent:
          maxLength: 22
          type: string
          description: Branch identification number.
        TellerIdent:
          maxLength: 80
          type: string
          description: Teller identification number.
        TillIdent:
          maxLength: 80
          type: string
          description: Till identification number.
        AMPMCode:
          type: string
          description: 'Transaction posting code. '
          enum:
          - AM
          - PM
        ReentryType:
          type: string
          description: Type of re-entry.
          enum:
          - Manual
          - Auto
        AdditionalSettings:
          type: object
          additionalProperties: true
          description: Contains additional information required to successfully process the transaction. Required By Cleartouch
    OvrdExceptionDataType:
      title: OvrdExceptionDataType
      type: object
      properties:
        OvrdAutoAckInd:
          type: boolean
          description: 
            Flag to indicate if the exceptions should be auto-acknowledged by the system. If this value is set to True, then any exception that can be overridden will be automatically acknowledged by the system and exceptions that cannot be overridden will be returned in the Status aggregate of the response message.
      description: Details of the exceptions that can be manually acknowledged by the client.
    OverrideExceptionType:
      title: OverrideExceptionType
      type: object
      properties:
        SubjectRole:
          type: string
          description: Role of the subject who is overriding the exception.
        SubjectIdent:
          maxLength: 36
          type: string
          description: Subject identification value to identify the subject who is overriding the exception.
        OvrdExceptionCode:
          maxLength: 40
          type: string
          description: Override exception code to be overridden. This can be the ServerStatusCode from the Message Status section.
        OvrdElement:
          $ref: '#/components/schemas/OvrdElementType'
    OvrdElementType:
      title: OvrdElementType
      type: object
      description: Information about the elements that corresponds to the override exception code.
      allOf:
      - $ref: '#/components/schemas/SubjectElementType'
      - type: object
    SubjectElementType:
      title: SubjectElementType
      type: object
      properties:
        Path:
          type: string
          description: Path of the element (in XPath absolute notation format) where the error occurred.  If the error occurred at the top-level element, then name of the element is returned in the response.
        ServerPath:
          maxLength: 256
          type: string
          description: Server Path is the Service Provider’s identification of the field in their schema, such as an XPath, field ID, or field name that is associated with the ServerStatusCode. If this element is set, it must be returned in the ServerPath element of the OvrdExceptionData aggregate if the exception is being overridden.
        Value:
          maxLength: 256
          type: string
          description: Human readable information of the EFX data value of the element to be overridden. This value should not be provided for the fields that cannot be represented as a string, for example, binary data.
      description: Information about the subject element
    StatusType:
      title: StatusType
      required:
      - Severity
      - StatusCode
      - StatusDesc
      - SvcProviderName
      type: object
      properties:
        Id:
          type: string
          description: Status identification number.
        StatusCode:
          maxLength: 20
          type: string
          description: EFX Standard Status code that indicates the result of API response. Refer to the <a href="../docs?path=docs/response-codes.md">Response Codes</a> section to view the list of all possible status codes.
        StatusDesc:
          maxLength: 255
          type: string
          description: Brief information about the EFX status code. Refer to the <a href="../docs?path=docs/response-codes.md">Response Codes</a> section for more information.
        Severity:
          $ref: '#/components/schemas/SeverityType'
        SvcProviderName:
          maxLength: 1024
          type: string
          description: |-
            Name of a service provider. Possible values are:
              -  Cleartouch
              -  DNA
              -  Precision
              -  Premier
              -  Signature
        ServerStatusCode:
          maxLength: 20
          type: string
          description: Server status code of the service provider's application.
        ServerStatusDesc:
          maxLength: 255
          type: string
          description: Description of the server status code of the service provider's application.
        OvrdExceptionInd:
          type: boolean
          description: Flag to indicate whether the exception can be overridden by resubmitting the request message.
        SubjectRole:
          type: string
          description: Authorization level required to override, such as Teller and Supervisor.
        SubjectElement:
          type: array
          description: Information about the elements that caused the status.
          items:
            $ref: '#/components/schemas/SubjectElementType'
        ContentHTML:
          type: string
          description: Response status in HTML format. This parameter returns only in some cases.
        AdditionalStatus:
          type: array
          description: Details of the additional status of the response message. This aggrigate can appear once for each additional status that the server is expected to provide.
          items:
            $ref: '#/components/schemas/AdditionalStatusType'
      description: Details of the API response messages.
    SeverityType:
      title: SeverityType
      type: string
      description: Severity type of the EFX status code. Refer to the <a href="../docs?path=docs/response-codes.md">Response Codes</a> section for more information.
      enum:
      - Error
      - Warning
      - Info
    AdditionalStatusType:
      title: AdditionalStatusType
      type: object
      properties:
        StatusCode:
          maxLength: 20
          type: string
          description: EFX Standard Status code that indicates the result of API response. Refer to the <a href="../docs?path=docs/response-codes.md">Response Codes</a> section to view the list of all possible status codes.
        StatusDesc:
          maxLength: 255
          type: string
          description: Brief information about the EFX status code. Refer to the <a href="../docs?path=docs/response-codes.md">Response Codes</a> for more information.
        Severity:
          $ref: '#/components/schemas/SeverityType'
        SvcProviderName:
          maxLength: 1024
          type: string
          description: |-
            Name of a service provider. Possible values are:
              - Cleartouch
              - DNA
              - Precision
              - Premier
              - Signature
        ServerStatusCode:
          maxLength: 20
          type: string
          description: 'Server status code of the service provider''s application.  '
        ServerStatusDesc:
          maxLength: 255
          type: string
          description: Description of the server status code of the service provider's application.
        OvrdExceptionInd:
          type: boolean
          description: Flag that indicates whether the exception can be overridden by resubmitting the request message.
        SubjectElement:
          type: array
          description: Information about the elements that caused the status.
          items:
            $ref: '#/components/schemas/SubjectElementType'
    XferAddOperRequest_XferInfo_FromAcctRef_AcctKeys:
      required:
      - AcctId
      type: object
      properties:
        AcctId:
          maxLength: 36
          type: string
          description: |+
            Unique identification number of an account record.





        AcctType:
          type: string
          description: |-
            Type of an account.

            For more information about account types, refer to the Account Types section in <a href="../docs?path=docs/api-glossary.md" target="_blank" title="Click to open in a new tab"> Glossary</a>.
          enum:
          - DDA
          - SDA
          - CDA
      description: |-
        Unique information to identify the account record.

        **Note:** This aggregate must be provided in the request along with AcctId and AcctType for the operation to work successfully. It has details of Account being Debited. ESF would return an error if AcctKeys/AcctId or AcctKeys/AcctType is not provided in request.
    XferAddOperRequest_XferInfo_FromAcctRef:
      required:
      - AcctKeys
      type: object
      properties:
        AcctKeys:
          $ref: '#/components/schemas/XferAddOperRequest_XferInfo_FromAcctRef_AcctKeys'
      description: Reference account information from which the funds are transferred.
    XferAddOperRequest_XferInfo_ToAcctRef_AcctKeys_AcctIdent:
      type: object
      properties:
        AcctIdentType:
          type: string
          description: "Type of account identifiers to identify the external transfer account.\n\n**Note:** Refers to external transfer account type. "
          enum:
          - AcctTypeCode
        AcctIdentValue:
          type: string
          description: "Value to identify the type of external transfer account.\n\n**Note:** *Conditionally Required field*\n\nRefers to external transfer account type. \n\n\n\nThis field is required when the transfer to account is an external transfer. A value of 'None' indicates no external account.\n\n\n\nWhen transferring from a DDA (Demand Deposit) account, the valid external account types are None, DDA, SDA, Vendor and LOAN. \n\n\n\nWhen transferring from a SDA (Savings Account) account, the valid external account types are None, DDA, SDA, Vendor, LOAN, Check.\n\n\n\nWhen transferring from a CDA account, the valid external account types are EXT, DDA, SDA, Check."
          enum:
          - None
          - DDA
          - SDA
          - LOAN
          - Vendor
          - Check
      description: Unique identifier of an account record used in case of external transfers.
    XferAddOperRequest_XferInfo_ToAcctRef_AcctKeys:
      required:
      - AcctId
      type: object
      properties:
        AcctId:
          maxLength: 36
          type: string
          description: Unique identification number of an account.
        AcctType:
          type: string
          description: |+
            Type of an account.



            For more information about account types, refer to the Account Types section in <a href="../docs?path=docs/api-glossary.md" target="_blank" title="Click to open in a new tab"> Glossary</a>.

            **Note:** EXT refers to external account.

          enum:
          - DDA
          - SDA
          - EXT
          - LOAN
          - SDB
          - GLA
        AcctIdent:
          $ref: '#/components/schemas/XferAddOperRequest_XferInfo_ToAcctRef_AcctKeys_AcctIdent'
        FIIdentType:
          type: string
          description: |-
            Identification type of a financial institution.

            Core uses Financial Institution Identification type for routing numbers.
          enum:
          - RoutingNum
        FIIdent:
          maxLength: 34
          type: string
          description: "Unique identification number of a financial institution. \n\n**Note:** *Conditionally Required field*\n\nRequired when the To Account Type is external."
      description: |-
        Unique account information used to identify the account record.

        **Note:** This aggregate must be provided in the request along with AcctId and AcctType for the operation to work successfully. It has details of Account being Credited. ESF would return an error if AcctKeys/AcctId or AcctKeys/AcctType is not provided in request.
    XferAddOperRequest_XferInfo_ToAcctRef:
      required:
      - AcctKeys
      type: object
      properties:
        AcctKeys:
          $ref: '#/components/schemas/XferAddOperRequest_XferInfo_ToAcctRef_AcctKeys'
        TrnCode:
          type: string
          description: "External transaction code used to indicate the type of transfer and the type of account.\n\n\n\n**Note:** For Premier, this field only applicable when the transfer TO account is a Loan account. "
      description: Reference information of an account to which funds are transferred.
    XferAddOperRequest_XferInfo_CurAmt_CurCode:
      required:
      - CurCodeValue
      type: object
      properties:
        CurCodeType:
          type: string
          description: "Standard ISO currency code format. \n\n\n\nRefer to the <a href=\"https://en.wikipedia.org/wiki/ISO_4217\" target=\"_blank\" title=\"Click to open in a new tab\">ISO 4217 codes </a> to view the list of all currency codes.\n\n**Note:** Premier allows ISO4217-Alpha type."
          enum:
          - ISO4217-Alpha
        CurCodeValue:
          type: string
          description: "Currency code value as per the ISO currency code format set in the CurCodeType field.\n\nFor example, currency code for the US Dollar in alpha and numeric formats are:\n\n- USD (in ISO4217-Alpha format) \n\n- 840 (in ISO4217-Numeric format)\n\n\n\nPresently only USD currency code is supported."
      description: Details of currency code.
    XferAddOperRequest_XferInfo_CurAmt:
      type: object
      properties:
        Amt:
          type: number
          description: Amount in decimal format.
        CurCode:
          $ref: '#/components/schemas/XferAddOperRequest_XferInfo_CurAmt_CurCode'
      description: Details of the amount to be transferred.
    XferAddOperRequest_XferInfo_RecurModel_RecurRule:
      properties:
        RecurType:
          type: string
          description: Type of payment cycle.
          enum:
          - Cycle
          - Monthly
          - TwiceMonthly
          - Quarterly
          - Yearly
          - Weekly
          - Once
          - Maturity
        RecurInterval:
          type: integer
          description: "Recurrence interval between two successive occurrences of transfer.\n\nFor example, if the recurrence type value is set to 'Monthly' and the recurrence interval set to '1', then there will be 1-month interval between the next statement.\n\nThe default value is set to 1.\n\n**Note:** Premier uses the Recur Interval with the Recur Type to determine the transfer frequency.  \n\n\n\nUse the following options:\n\n- If recurrence type value is once, no interval value is required\n\n- If recurrence type value is months, interval use values \"1 \" for Monthly, \"3\" for Quarterly, \"2\" for BiMonthly or \"6\" for SemiAnnually intervals\n\nAlso: \n\n- For Recurrence Type value based on a quarter can also be expressed using \"1\" for Quarterly.\n\n- For Recurrence Type  based on weeks use this field to express \"1\"  for Weekly or \"2\" for BiWeekly.\n\n- For Recurrence Type based on years, use value \"1\"  for Yearly.\n\n- For Recurrence Type values based on twice monthly use \"1\" for TwiceMonthly.\n\n\n\n\n\n\n\n\n\n\n\n\n\n"
          format: int64
        RecurStartDate:
          type: string
          description: |-
            Date at which the recurrence cycle of transfer is started.



            Format: yyyy-mm-dd

            Example: 2021-10-20
          format: date
        Occurrences:
          type: integer
          description: "Total number of transfer occurrences.\n\n**Note:** Refers to transfers remaining. This may be used instead of RecurEndDate.\n\nConsumer application can only send occurrences or XferRecurEndDate for add operation. "
          format: int64
        RecurEndDate:
          type: string
          description: "Ending date or expiry date for the recurrence cycle of transfer.\n\n\n\nFormat: mm/dd/yyyy\n\n**Note:** This may be used instead of Occurrences. Consumer application can only send occurrences or XferRecurEndDate for add operation. "
          format: date
    XferAddOperRequest_XferInfo_RecurModel:
      type: object
      properties:
        RecurRule:
          type: array
          description: Details of the recurrence rule.
          items:
            $ref: '#/components/schemas/XferAddOperRequest_XferInfo_RecurModel_RecurRule'
      description: Details of recurring time-based payment module for an automatic transfer of funds.
    XferAddOperRequest_XferInfo_RelationshipMgr:
      required:
      - RelationshipMgrIdent
      properties:
        RelationshipMgrIdent:
          maxLength: 36
          type: string
          description: Unique identifier of a relationship manager.
        RelationshipRole:
          type: string
          description: "Role of a relationship manager.\n\n**Note:** Officer refers to responsibility code.  "
          enum:
          - Officer
    XferAddOperRequest_XferInfo_ExtendedXferData_NoticeData:
      properties:
        NoticeType:
          type: string
          description: |-
            Information used to identify the type of notice.

            Core can have both regular as well as ACH notice type.
          enum:
          - RegularNotice
          - ACHNotice
        NoticeOption:
          type: string
          description: Notification code that indicates whether the transfer notification is applicable for the account.
    XferAddOperRequest_XferInfo_ExtendedXferData:
      type: object
      properties:
        XferAmtCode:
          type: string
          description: Code that indicates whether the next transfer amount is fixed or variable.
          enum:
          - Fixed
          - Variable
        ACHEntryClass:
          type: string
          description: Code used to identify the NACHA payment type (standard entry class).
        NoticeData:
          type: array
          description: Details used for notice generation each time when a transfer takes place.
          items:
            $ref: '#/components/schemas/XferAddOperRequest_XferInfo_ExtendedXferData_NoticeData'
        ForcePostInd:
          type: boolean
          description: Flag to indicate whether to process a transfer using the force pay transaction code.
        FeeIdent:
          maxLength: 36
          type: string
          description: |-
            Identification code used to identify fee applicable for a transfer.

            **Note:** Tag requires pre installation tasks to define the values for the field.

            Refers to the Transfer Charge Code that identifies one of 999 transfer fee specifications to be automatically assessed when the transfer occurs.
      description: Details of additional information used to add instructions for the fund transfer.
    XferAddOperRequest_XferInfo_RefData:
      required:
      - RefType
      properties:
        RefType:
          type: string
          description: |+
            Type of reference information used.

            **Note:** PayorDesc refers to Payor identification in Premier. PayorDesc is required when the ‘To Account’ type is Ext (External account) and External account type is DDA,SDA,LOAN.



            PayeeDesc refers to Vendor number in Premier. VendorNumber is required when the ‘To Account’ type is Ext (External account) and External account type is Vendor(BPM).





          enum:
          - PayeeDesc
          - PayorDesc
        RefIdent:
          maxLength: 36
          type: string
          description: |+
            Unique Identification number of reference information. This field in combination with reference type is used to identify the reference data.

    XferAddOperRequest_XferInfo:
      type: object
      properties:
        FromAcctRef:
          $ref: '#/components/schemas/XferAddOperRequest_XferInfo_FromAcctRef'
        ToAcctRef:
          $ref: '#/components/schemas/XferAddOperRequest_XferInfo_ToAcctRef'
        CurAmt:
          $ref: '#/components/schemas/XferAddOperRequest_XferInfo_CurAmt'
        RecurModel:
          $ref: '#/components/schemas/XferAddOperRequest_XferInfo_RecurModel'
        RelationshipMgr:
          type: array
          description: Information about the financial institution officers who have management-related responsibilities of the account.
          items:
            $ref: '#/components/schemas/XferAddOperRequest_XferInfo_RelationshipMgr'
        ReportGroupCode:
          type: string
          description: |-
            Client-defined code to group the accounts for reporting purposes.



            **Note:** Tag requires pre installation tasks to define the values for the field.
        ExpediteInd:
          type: boolean
          description: |+
            Flag to indicate the fund transfer execution subject to service profile. If the indicator is 'true', then the transfer is an immediate transfer and by default the value is set to false.

            **Note:** In Premier, immediate transfers are executed by the TMS services and the recurring transfers are executed by PRM services. This indicator will determine which services are used.

        ExtendedXferData:
          $ref: '#/components/schemas/XferAddOperRequest_XferInfo_ExtendedXferData'
        XferFromDesc:
          maxLength: 80
          type: array
          description: |+
            Mandatory description used for a transfer posted by the account from where the funds are transferred.

            Value in this field should be identical to the value provided in XferToDesc.

          items: {}
        XferToDesc:
          maxLength: 80
          type: array
          description: |+
            Mandatory description used for a transfer posted for the account to where the funds are transferred.

            Value in this field should be identical to the value provided in XferFromDesc.

          items: {}
        RefData:
          type: array
          description: Details of reference information used to correlate to transactions provided in bank statements.
          items:
            $ref: '#/components/schemas/XferAddOperRequest_XferInfo_RefData'
      description: Details of an automatic fund transfer.
    XferAddOperResponse_XferStatusRec_XferKeys:
      type: object
      properties:
        XferId:
          maxLength: 36
          type: string
          description: |-
            Unique identification number of transfer record.

            **Note:** Refers to the addenda number.
      description: Unique information to identify the transaction record.
    XferAddOperResponse_XferStatusRec_XferStatus:
      type: object
      properties:
        EffDt:
          pattern: '[0-9]{4}-[0-9]{2}-[0-9]{2}(T[0-9]{2}:[0-9]{2}(:[0-9]{2}(\.[0-9]{3})?((-|\+)[0-9]{2}:[0-9]{2})?)?)?'
          type: string
          description: |+
            Effective date and time when transfer status is changed.



            Format: yyyy-MM-ddTHH:mm:ss.SSS

            Example: 2021-01-31T23:59:59.999



      description: Current status of the transfer.
    XferAddOperResponse_XferStatusRec:
      required:
      - XferKeys
      - XferStatus
      type: object
      properties:
        XferKeys:
          $ref: '#/components/schemas/XferAddOperResponse_XferStatusRec_XferKeys'
        XferStatus:
          $ref: '#/components/schemas/XferAddOperResponse_XferStatusRec_XferStatus'
      description: Fund transfer status details used in a response message to return results from an add, modify or delete operation.
    SecuredXferDelOperRequest_XferKeys_AcctKeys:
      required:
      - AcctId
      type: object
      properties:
        AcctId:
          maxLength: 36
          type: string
          description: |+
            Unique identification number of an account record.





        AcctType:
          type: string
          description: |-
            Type of an account.

            For more information about account types, refer to the Account Types section in <a href="../docs?path=docs/api-glossary.md" target="_blank" title="Click to open in a new tab"> Glossary</a>.
          enum:
          - DDA
          - SDA
          - CDA
      description: Unique information to identify the account record.
    SecuredXferDelOperRequest_XferKeys:
      required:
      - AcctKeys
      type: object
      properties:
        AcctKeys:
          $ref: '#/components/schemas/SecuredXferDelOperRequest_XferKeys_AcctKeys'
        XferIdent:
          maxLength: 36
          type: string
          description: |-
            Unique identifier of a transfer record.

            **Note:** Refers to the addenda number.
      description: |+
        Unique information to identify the transaction record.

        **Note:** Transfer identifier for service provider is combination of account number, account type and transfer addenda number.



    SecuredXferDelOperResponse_XferStatusRec_XferKeys:
      required:
      - AcctKeys
      type: object
      properties:
        AcctKeys:
          $ref: '#/components/schemas/SecuredXferDelOperRequest_XferKeys_AcctKeys'
        XferIdent:
          maxLength: 36
          type: string
          description: Unique identifier of a transfer record.
      description: Unique information to identify the transaction record.
    SecuredXferDelOperResponse_XferStatusRec_XferStatus:
      required:
      - XferStatusCode
      type: object
      properties:
        XferStatusCode:
          type: string
          description: Code to identify the status of the transfer.
        EffDt:
          pattern: '[0-9]{4}-[0-9]{2}-[0-9]{2}(T[0-9]{2}:[0-9]{2}(:[0-9]{2}(\.[0-9]{3})?((-|\+)[0-9]{2}:[0-9]{2})?)?)?'
          type: string
          description: |+
            Effective date and time when transfer status is changed.



            Format: yyyy-MM-ddTHH:mm:ss.sss

            Example: 2021-01-31T23:59:59.999



      description: Current status of the transfer.
    SecuredXferDelOperResponse_XferStatusRec:
      required:
      - XferKeys
      - XferStatus
      type: object
      properties:
        XferKeys:
          $ref: '#/components/schemas/SecuredXferDelOperResponse_XferStatusRec_XferKeys'
        XferStatus:
          $ref: '#/components/schemas/SecuredXferDelOperResponse_XferStatusRec_XferStatus'
      description: Fund transfer status details used in a response message to return results from an add, modify or delete operation
    SecuredXferInqOperRequest_XferSel:
      type: object
      properties:
        XferKeys:
          $ref: '#/components/schemas/SecuredXferDelOperResponse_XferStatusRec_XferKeys'
      description: Selection criteria to retrieve the transfer records.
    SecuredXferInqOperResponse_XferInfo_FromAcctRef:
      required:
      - AcctKeys
      type: object
      properties:
        AcctKeys:
          $ref: '#/components/schemas/SecuredXferDelOperRequest_XferKeys_AcctKeys'
      description: Reference account information from which the funds are transferred.
    SecuredXferInqOperResponse_XferInfo_ToAcctRef_AcctKeys_AcctIdent:
      type: object
      properties:
        AcctIdentType:
          type: string
          description: Type of account identifiers to identify the external transfer account.
          enum:
          - AcctTypeCode
        AcctIdentValue:
          maxLength: 36
          type: string
          description: |+
            Value to identify the type of external transfer account.

            **Note:** *Conditionally Required field*

            Required if the Credit To Account Type is external.

            - 0: None

            - 1: Demand Deposit

            - 2: Savings

            - 3:Vendor (BPM)

            - 4: Loan

      description: Unique identifier of an account record used in case of external transfers.
    SecuredXferInqOperResponse_XferInfo_ToAcctRef_AcctKeys:
      required:
      - AcctId
      type: object
      properties:
        AcctId:
          maxLength: 36
          type: string
          description: |+
            Unique identification number of an account record.





        AcctType:
          type: string
          description: |-
            Type of an account.

            For more information about account types, refer to the Account Types section in <a href="../docs?path=docs/api-glossary.md" target="_blank" title="Click to open in a new tab"> Glossary</a>.
          enum:
          - DDA
          - SDA
          - EXT
          - LOAN
          - SDB
          - GLA
        AcctIdent:
          $ref: '#/components/schemas/SecuredXferInqOperResponse_XferInfo_ToAcctRef_AcctKeys_AcctIdent'
        FIIdentType:
          type: string
          description: |-
            Identification type of a financial institution.

            Core uses financial institution identification type for routing numbers.
          enum:
          - RoutingNum
        FIIdent:
          maxLength: 34
          type: string
          description: "Unique identification number of a financial institution. \n\n**Note:** *Conditionally Required field*\n\nRequired when the To account type is external."
      description: Unique information to identify the account record.
    SecuredXferInqOperResponse_XferInfo_ToAcctRef:
      required:
      - AcctKeys
      type: object
      properties:
        AcctKeys:
          $ref: '#/components/schemas/SecuredXferInqOperResponse_XferInfo_ToAcctRef_AcctKeys'
      description: Reference information of an account to which funds are transferred.
    SecuredXferInqOperResponse_XferInfo_CurAmt_CurCode:
      required:
      - CurCodeValue
      type: object
      properties:
        CurCodeType:
          type: string
          description: "Standard ISO currency code format. \n\n\n\nRefer to the <a href=\"https://en.wikipedia.org/wiki/ISO_4217\" target=\"_blank\" title=\"Click to open in a new tab\">ISO 4217 codes </a> to view the list of all currency codes."
          enum:
          - ISO4217-Alpha
        CurCodeTypeEnumDesc:
          maxLength: 80
          type: string
          description: Description of the enumeration value in the CurCodeType field.
        CurCodeValue:
          type: string
          description: "Currency code value as per the ISO currency code format set in the CurCodeType field.\n\nFor example, currency code for the US Dollar in alpha and numeric formats are:\n\n- USD (in ISO4217-Alpha format) \n\n- 840 (in ISO4217-Numeric format)\n\n\n\nPresently only USD currency code is supported."
      description: Details of currency code.
    SecuredXferInqOperResponse_XferInfo_CurAmt:
      type: object
      properties:
        Amt:
          type: number
          description: Amount in decimal format.
        CurCode:
          $ref: '#/components/schemas/SecuredXferInqOperResponse_XferInfo_CurAmt_CurCode'
      description: Details of the amount to be transferred.
    SecuredXferInqOperResponse_XferInfo_RecurModel_RecurRule:
      properties:
        RecurType:
          type: string
          description: Type of payment cycle.
        RecurTypeEnumDesc:
          maxLength: 80
          type: string
          description: Description of enumeration value in the RecurType field.
        RecurInterval:
          type: integer
          description: |-
            Recurrence interval between two successive occurrences of transfer.

            For example, if the recurrence type value is set to 'Monthly' and the recurrence interval set to '1', then there will be 1-month interval between the next statement.

            The default value is set to 1.
          format: int64
        DayOfMonth:
          type: string
          description: |-
            Day of the month to define the day of transfer in the month. This field is used when transfer posting period is monthly or SemiMonthly.



            A value of 1 in this field means that the transfer will occur on the first day of every month. The value of 31 means that the transfer will occur on the last day of the month.
        RecurStartDate:
          type: string
          description: |-
            Date at which the recurrence cycle of transfer is started.



            Format: yyyy-mm-dd.

            Example: 2021-10-20.
          format: date
        RecurEndDate:
          type: string
          description: |-
            Ending date or expiry date for the recurrence cycle of transfer.



            Format: yyyy-mm-dd.

            Example: 2021-10-20.
          format: date
    SecuredXferInqOperResponse_XferInfo_RecurModel:
      type: object
      properties:
        RecurRule:
          type: array
          description: Details of the recurrence rule.
          items:
            $ref: '#/components/schemas/SecuredXferInqOperResponse_XferInfo_RecurModel_RecurRule'
      description: Details of recurring time-based payment module for an automatic transfer of funds.
    SecuredXferInqOperResponse_XferInfo_RelationshipMgr:
      required:
      - RelationshipMgrIdent
      properties:
        RelationshipMgrIdent:
          maxLength: 36
          type: string
          description: Unique identifier of a relationship manager.
        RelationshipMgrIdentEnumDesc:
          maxLength: 80
          type: string
          description: Description of enumeration value in the RelationshipMgrIdent field.
        RelationshipRole:
          type: string
          description: Role of a relationship manager.
          enum:
          - Officer
        RelationshipRoleEnumDesc:
          maxLength: 80
          type: string
          description: Description of enumeration value in the RelationshipRole field.
    SecuredXferInqOperResponse_XferInfo_ExtendedXferData_NoticeData:
      properties:
        NoticeType:
          type: string
          description: |-
            Information used to identify the type of notice.

            Core can have both regular as well as ACH notice type.
          enum:
          - RegularNotice
          - ACHNotice
        NoticeTypeEnumDesc:
          maxLength: 80
          type: string
          description: Description of enumeration value in the NoticeType field.
        NoticeOption:
          type: string
          description: Notification code that indicates whether the transfer notification is applicable for the account.
          enum:
          - NoNotice
          - GenerateNotice
        NoticeOptionEnumDesc:
          maxLength: 80
          type: string
          description: Description of enumeration value in the NoticeOption field.
    SecuredXferInqOperResponse_XferInfo_ExtendedXferData:
      type: object
      properties:
        ACHEntryClass:
          type: string
          description: Code used to identify the NACHA payment type (standard entry class).
          enum:
          - PPD
          - CCD
          - WEB
          - CIE
        NoticeData:
          type: array
          description: Details used for notice generation each time when a transfer takes place.
          items:
            $ref: '#/components/schemas/SecuredXferInqOperResponse_XferInfo_ExtendedXferData_NoticeData'
        ForcePostInd:
          type: boolean
          description: Flag to indicate whether to process a transfer using the force pay transaction code.
        FeeIdent:
          maxLength: 36
          type: string
          description: |-
            Identification code used to identify fee applicable for a transfer.

            **Note:** Values are client-defined. Refers to the transfer charge code that identifies one of 999 transfer fee specifications to be automatically assessed when the transfer occurs.
      description: Details of additional information used to add instructions for the fund transfer.
    SecuredXferInqOperResponse_XferInfo_RefData:
      required:
      - RefType
      properties:
        RefType:
          type: string
          description: "Type of reference information used.\n\n**Note:** \n\n- Value \"XferDesc\" refers to the description of the transfer. \n\n- Value \"PayeeDesc\" refers to the payor identification."
          enum:
          - PayeeDesc
          - PayorDesc
          - XferDesc
          - TranCode
        RefTypeEnumDesc:
          maxLength: 80
          type: string
          description: Description of enumeration value in the RefType field.
        RefIdent:
          maxLength: 36
          type: string
          description: |-
            Unique Identification number of reference information. This field in combination with reference type is used to identify the reference data.



            **Note:** Refers to the vendor number.
        RefDesc:
          maxLength: 1024
          type: string
          description: Description of the identifier.
    SecuredXferInqOperResponse_XferInfo:
      type: object
      properties:
        FromAcctRef:
          $ref: '#/components/schemas/SecuredXferInqOperResponse_XferInfo_FromAcctRef'
        ToAcctRef:
          $ref: '#/components/schemas/SecuredXferInqOperResponse_XferInfo_ToAcctRef'
        CurAmt:
          $ref: '#/components/schemas/SecuredXferInqOperResponse_XferInfo_CurAmt'
        RecurModel:
          $ref: '#/components/schemas/SecuredXferInqOperResponse_XferInfo_RecurModel'
        RelationshipMgr:
          type: array
          description: Information about the financial institution officers who have management-related responsibilities of the account.
          items:
            $ref: '#/components/schemas/SecuredXferInqOperResponse_XferInfo_RelationshipMgr'
        ReportGroupCode:
          type: string
          description: |+
            Client-defined code to group the accounts for reporting purposes.

        ExtendedXferData:
          $ref: '#/components/schemas/SecuredXferInqOperResponse_XferInfo_ExtendedXferData'
        RefData:
          type: array
          description: Details of reference information used to correlate to transactions provided in bank statements.
          items:
            $ref: '#/components/schemas/SecuredXferInqOperResponse_XferInfo_RefData'
        RemainingOccurrences:
          type: integer
          description: Number of transfer occurrences that are pending
          format: int64
      description: Details of an automatic fund transfer.
    SecuredXferInqOperResponse_XferRec:
      required:
      - XferInfo
      - XferKeys
      - XferStatus
      properties:
        XferKeys:
          $ref: '#/components/schemas/SecuredXferDelOperResponse_XferStatusRec_XferKeys'
        XferInfo:
          $ref: '#/components/schemas/SecuredXferInqOperResponse_XferInfo'
        XferStatus:
          $ref: '#/components/schemas/SecuredXferDelOperResponse_XferStatusRec_XferStatus'
    XferListInqRequest_XferListSel_AcctRef:
      required:
      - AcctKeys
      type: object
      properties:
        AcctKeys:
          $ref: '#/components/schemas/SecuredXferDelOperRequest_XferKeys_AcctKeys'
      description: Reference information of an account object involved in the transfer.
    XferListInqRequest_XferListSel:
      type: object
      properties:
        AcctRef:
          $ref: '#/components/schemas/XferListInqRequest_XferListSel_AcctRef'
      description: Selection criteria to retrieve the transfer list record.
    XferListInqResponse_RecCtrlOut:
      required:
      - SentRecCount
      type: object
      properties:
        SentRecCount:
          type: string
          description: Total number of records that matched the selection criteria and returned in the response message.
          enum:
          - 1-999
      description: |-
        Information retrieved for the criteria specified in the RecCtrlIn aggregate of the request message.



        **Note:** Service provider does not support record control for transfer list inquiry. Up to 999 transfer records can be returned in the response. All transfer addendums available on the account will be returned on the response.
    XferListInqResponse_XferKeys:
      required:
      - AcctKeys
      type: object
      properties:
        AcctKeys:
          $ref: '#/components/schemas/SecuredXferDelOperRequest_XferKeys_AcctKeys'
        XferIdent:
          type: string
          description: |-
            Unique identifier of a transfer record.

            **Note:** Refers to addenda number.
          enum:
          - 1-999
      description: Unique information to identify the transaction record.
    XferListInqResponse_XferListInfo_ToAcctRef_AcctKeys:
      required:
      - AcctId
      type: object
      properties:
        AcctId:
          maxLength: 36
          type: string
          description: |+
            Unique identification number of an account record.





        AcctType:
          type: string
          description: |-
            Type of an account.

            For more information about account types, refer to the Account Types section in <a href="../docs?path=docs/api-glossary.md" target="_blank" title="Click to open in a new tab"> Glossary</a>.

            **Note:** Value "EXT" refers to external account.
          enum:
          - DDA
          - SDA
          - EXT
          - LOAN
          - SDB
          - GLA
      description: Unique information to identify the account record.
    XferListInqResponse_XferListInfo_ToAcctRef:
      required:
      - AcctKeys
      type: object
      properties:
        AcctKeys:
          $ref: '#/components/schemas/XferListInqResponse_XferListInfo_ToAcctRef_AcctKeys'
      description: Reference information of an account to which funds are transferred.
    XferListInqResponse_XferListInfo_CurAmt_CurCode:
      required:
      - CurCodeValue
      type: object
      properties:
        CurCodeType:
          type: string
          description: "Standard ISO currency code format. \n\n\n\nRefer to the <a href=\"https://en.wikipedia.org/wiki/ISO_4217\" target=\"_blank\" title=\"Click to open in a new tab\">ISO 4217 codes </a> to view the list of all currency codes."
        CurCodeTypeEnumDesc:
          maxLength: 80
          type: string
          description: Description of the enumeration value in the CurCodeType field.
        CurCodeValue:
          type: string
          description: "Currency code value as per the ISO currency code format set in the CurCodeType field.\n\nFor example, currency code for the US Dollar in alpha and numeric formats are:\n\n- USD (in ISO4217-Alpha format) \n\n- 840 (in ISO4217-Numeric format)\n\n\n\nPresently only USD currency code is supported."
      description: Details of currency code.
    XferListInqResponse_XferListInfo_CurAmt:
      type: object
      properties:
        Amt:
          type: number
          description: Amount in decimal format.
        CurCode:
          $ref: '#/components/schemas/XferListInqResponse_XferListInfo_CurAmt_CurCode'
      description: Details of the amount to be transferred.
    XferListInqResponse_XferListInfo_RecurModel_RecurRule:
      properties:
        RecurType:
          type: string
          description: Type of payment cycle.
          enum:
          - Cycle
          - Monthly
          - Quarterly
          - Yearly
          - Weekly
          - TwiceMonthly
          - Once
          - InterestCycle
          - Maturity
        RecurInterval:
          type: integer
          description: |-
            Recurrence interval between two successive occurrences of transfer.

            For example, if the recurrence type value is set to 'Monthly' and the recurrence interval set to '1', then there will be 1-month interval between the next statement.

            The default value is set to 1.
          format: int64
    XferListInqResponse_XferListInfo_RecurModel:
      type: object
      properties:
        RecurRule:
          type: array
          description: Details of the recurrence rule.
          items:
            $ref: '#/components/schemas/XferListInqResponse_XferListInfo_RecurModel_RecurRule'
      description: Details of recurring time-based payment module for an automatic transfer of funds.
    XferListInqResponse_XferListInfo:
      required:
      - CurAmt
      type: object
      properties:
        FromAcctRef:
          $ref: '#/components/schemas/SecuredXferInqOperResponse_XferInfo_FromAcctRef'
        ToAcctRef:
          $ref: '#/components/schemas/XferListInqResponse_XferListInfo_ToAcctRef'
        CurAmt:
          $ref: '#/components/schemas/XferListInqResponse_XferListInfo_CurAmt'
        StartDt:
          pattern: '[0-9]{4}-[0-9]{2}-[0-9]{2}(T[0-9]{2}:[0-9]{2}(:[0-9]{2}(\.[0-9]{3})?((-|\+)[0-9]{2}:[0-9]{2})?)?)?'
          type: string
          description: |-
            Start date and time of the fund transfer.

            Format: yyyy-MM-ddTHH:mm:ss.sss

            Example: 2021-01-31T23:59:59.999
        EndDt:
          pattern: '[0-9]{4}-[0-9]{2}-[0-9]{2}(T[0-9]{2}:[0-9]{2}(:[0-9]{2}(\.[0-9]{3})?((-|\+)[0-9]{2}:[0-9]{2})?)?)?'
          type: string
          description: |-
            End date and time value of the fund transfer.

            Format: yyyy-MM-ddTHH:mm:ss.sss

            Example: 2021-01-31T23:59:59.999
        RecurModel:
          $ref: '#/components/schemas/XferListInqResponse_XferListInfo_RecurModel'
        XferAmtCode:
          type: string
          description: |-
            Code that indicates whether the next transfer amount is fixed or variable.

            **Note:** Refers to transfer amount code.
          enum:
          - Fixed
          - Variable
          - IntPlusDistribution
          - BalanceMinusDistribution
        Nickname:
          maxLength: 80
          type: string
          description: Name that customer can assign for easy identification.
      description: Details of the funds transfer.
    XferListInqResponse_XferListRec:
      required:
      - XferKeys
      - XferListInfo
      - XferStatus
      properties:
        XferKeys:
          $ref: '#/components/schemas/XferListInqResponse_XferKeys'
        XferListInfo:
          $ref: '#/components/schemas/XferListInqResponse_XferListInfo'
        XferStatus:
          $ref: '#/components/schemas/SecuredXferDelOperResponse_XferStatusRec_XferStatus'
    XferModOperRequest_XferKeys:
      required:
      - AcctKeys
      type: object
      properties:
        AcctKeys:
          $ref: '#/components/schemas/SecuredXferDelOperRequest_XferKeys_AcctKeys'
        XferIdent:
          maxLength: 36
          type: string
          description: |-
            Unique identifier of a transfer record.

            **Note:** Refers to addenda number.
      description: |-
        Unique information to identify the transaction record.

        **Note:** Transfer identifier for the service provider is a combination of account number that is used to debit account for the transfer, account type and addenda number.
    XferModOperRequest_XferInfo_FromAcctRef_AcctKeys:
      required:
      - AcctId
      type: object
      properties:
        AcctId:
          maxLength: 36
          type: string
          description: |-
            Unique identification number of an account record.







            **Note:** Debit account number cannot be updated. Debit account number is part of the transfer identifier. If a different debit account number needs to be used for the transfer, new transfer request will need to be created using desired debit account number as part of the transfer identifier.
        AcctType:
          type: string
          description: |-
            Type of an account.

            For more information about account types, refer to the Account Types section in <a href="../docs?path=docs/api-glossary.md" target="_blank" title="Click to open in a new tab"> Glossary</a>.

            **Note:** Debit account type cannot be updated. Debit account type is part of the transfer identifier. If a different debit account type needs to be used for the transfer, new transfer request will need to be created using desired debit account type as part of the transfer identifier.
          enum:
          - DDA
          - SDA
          - CDA
      description: Unique information to identify the account record.
    XferModOperRequest_XferInfo_FromAcctRef:
      required:
      - AcctKeys
      type: object
      properties:
        AcctKeys:
          $ref: '#/components/schemas/XferModOperRequest_XferInfo_FromAcctRef_AcctKeys'
      description: Reference account information from which the funds are transferred.
    XferModOperRequest_XferInfo_ToAcctRef_AcctKeys_AcctIdent:
      type: object
      properties:
        AcctIdentType:
          type: string
          description: Type of account identifiers to identify the external transfer account.
          enum:
          - AcctTypeCode
        AcctIdentValue:
          type: string
          description: |+
            Value to identify the type of external transfer account.

            **Note:** Refers to external transfer account type. Required if the credit to account type is external.

            Valid values for DDA:

            - 0 = None

            - 1 = Demand Deposit

            - 2 = Savings

            - 3 = Vendor (BPM)

            - 4 = Loan

            Valid values for SDA:

            - 0 = None

            - 1 = Demand Deposit

            - 2 = Savings

            - 3 = Vendor (BPM)

            - 4 = Loan

            - 9 =Check

            Valid values for CDA:

            - 0 = None

            - 1 = Demand Deposit

            - 2 = Savings

            - 9 =Check



          enum:
          - "0"
          - "1"
          - "2"
          - "3"
          - "4"
          - "9"
      description: Unique identifier of an account record used in case of external transfers.
    XferModOperRequest_XferInfo_ToAcctRef_AcctKeys:
      required:
      - AcctId
      type: object
      properties:
        AcctId:
          maxLength: 36
          type: string
          description: Unique identification number of an account.
        AcctType:
          type: string
          description: |-
            Type of an account.

            For more information about account types, refer to the Account Types section in <a href="../docs?path=docs/api-glossary.md" target="_blank" title="Click to open in a new tab"> Glossary</a>.

            **Note:** Value "EXT" refers to external account.
          enum:
          - DDA
          - SDA
          - EXT
          - LOAN
          - SDB
          - GLA
        AcctIdent:
          $ref: '#/components/schemas/XferModOperRequest_XferInfo_ToAcctRef_AcctKeys_AcctIdent'
        FIIdentType:
          type: string
          description: |-
            Identification type of a financial institution.

            Core uses financial institution Identification type for routing numbers.
          enum:
          - RoutingNum
        FIIdent:
          maxLength: 34
          type: string
          description: "Unique identification number of a financial institution. \n\n**Note:** *Conditionally Required field*\n\nRequired if the To account type is external."
      description: Unique account information used to identify the account record.
    XferModOperRequest_XferInfo_ToAcctRef:
      required:
      - AcctKeys
      type: object
      properties:
        AcctKeys:
          $ref: '#/components/schemas/XferModOperRequest_XferInfo_ToAcctRef_AcctKeys'
      description: Reference information of an account to which funds are transferred.
    XferModOperRequest_XferInfo_CurAmt_CurCode:
      required:
      - CurCodeValue
      type: object
      properties:
        CurCodeType:
          type: string
          description: "Standard ISO currency code format. \n\n\n\nRefer to the <a href=\"https://en.wikipedia.org/wiki/ISO_4217\" target=\"_blank\" title=\"Click to open in a new tab\">ISO 4217 codes </a> to view the list of all currency codes."
          enum:
          - ISO4217-Alpha
        CurCodeValue:
          type: string
          description: "Currency code value as per the ISO currency code format set in the CurCodeType field.\n\nFor example, currency code for the US Dollar in alpha and numeric formats are:\n\n- USD (in ISO4217-Alpha format) \n\n- 840 (in ISO4217-Numeric format)\n\n\n\nPresently only USD currency code is supported."
      description: Details of currency code.
    XferModOperRequest_XferInfo_CurAmt:
      type: object
      properties:
        Amt:
          type: number
          description: Amount in decimal format.
        CurCode:
          $ref: '#/components/schemas/XferModOperRequest_XferInfo_CurAmt_CurCode'
      description: Details of the amount to be transferred.
    XferModOperRequest_XferInfo_RecurModel_RecurRule:
      properties:
        RecurType:
          type: string
          description: Type of payment cycle.
          enum:
          - Cycle
          - Monthly
          - Quarterly
          - Yearly
          - Weekly
          - TwiceMonthly
          - Once
          - InterestCycle
          - Maturity
        RecurInterval:
          type: integer
          description: "Recurrence interval between two successive occurrences of transfer.\n\nFor example, if the recurrence type value is set to 'Monthly' and the recurrence interval set to '1', then there will be 1-month interval between the next statement.\n\nThe default value is set to 1.\n\n**Note:** Use the following options: \n\n- If recurrence type is \"Once\", Interest Cycle or Maturity no interval is required\n\n- If recurrence type based on Cycle use values 001 -999 to express the cycle number \n\n- If recurrence type based on Months use values '1' for Monthly, '3' for Quarterly, '2' for BiMonthly or '6' for SemiAnnually\n\n- If recurrence type based on Quarter use values '1' for Quarterly\n\n- If recurrence type based on Week use values '1' for Weekly or '2' for BiWeekly\n\n- If recurrence type based on Year, use value1 for Yearly\n\n\n\n \n\n\n\n"
          format: int64
        DayOfMonth:
          type: string
          description: |-
            Day of the month to define the day of transfer in the month. This field is used when transfer posting period is Monthly or SemiMonthly.

            A value of 1 in this field means that the transfer will occur on the first day of every month. The value of 31 means that the transfer will occur on the last day of the month.

            **Note:** Refers to transfer day.

            Required for twice monthly frequency.

            If the Transfer Frequency is 'TwiceMonthly', the valid values are:

            - '1' 1st and 16th of the month.

            - '2' 2nd and 17th of the month.

            - '3' 3rd and 18th of the month.

            - '4' 4th and 19th of the month.

            - '5' 5th and 20th of the month.

            - '6' 6th and 21th of the month.

            - '7' 7th and 22th of the month.

            - '8' 8th and 23th of the month.

            - '9' 9th and 24th of the month.

            - '10' 10th and 25th of the month.

            - '11' 11th and 26th of the month.

            - '12' 12th and 27th of the month.

            - '13' 13th and 28th of the month.

            - '14' 14th and 29th of the month.

            - '15' 15th and 30th of the month (end of month in 31  day months).

            Twice monthly values represent the first day within a month that the transfer occurs.
        RecurStartDate:
          type: string
          description: |-
            Date at which the recurrence cycle of transfer is started.



            Format: yyyy-mm-dd.

            Example: 2021-10-20.

            **Note:** Refers to next transfer date. Service provider supports cycle or next transfer date when modifying a transfer.

            Cannot be sent if recurrence type is cycle.
          format: date
        RecurEndDate:
          type: string
          description: |-
            Ending date or expiry date for the recurrence cycle of transfer.



            Format: mm/dd/yyyy.

            Example: 10/20/2021.

            **Note:** This may be used instead of occurrences.
          format: date
    XferModOperRequest_XferInfo_RecurModel:
      type: object
      properties:
        RecurRule:
          type: array
          description: Details of the recurrence rule.
          items:
            $ref: '#/components/schemas/XferModOperRequest_XferInfo_RecurModel_RecurRule'
      description: Details of recurring time-based payment module for an automatic transfer of funds.
    XferModOperRequest_XferInfo_RelationshipMgr:
      required:
      - RelationshipMgrIdent
      properties:
        RelationshipMgrIdent:
          maxLength: 36
          type: string
          description: |-
            Unique identifier of a relationship manager.

            **Note:** Refers to responsibility code.

            The client-defined responsibility code identifies the officer responsible for the account. Values are 00000-99999.
        RelationshipRole:
          type: string
          description: Role of a relationship manager.
          enum:
          - Officer
    XferModOperRequest_XferInfo_ExtendedXferData_NoticeData:
      properties:
        NoticeType:
          type: string
          description: |-
            Information used to identify the type of notice.

            Core can have both regular as well as ACH notice type.

            **Note:** ACHNotice refers to ACH Pre-Note sent flag.

            RegularNotice refers to notification code.
          enum:
          - ACHNotice
          - RegularNotice
        NoticeOption:
          type: string
          description: Notification code that indicates whether the transfer notification is applicable for the account.
    XferModOperRequest_XferInfo_ExtendedXferData:
      type: object
      properties:
        XferAmtCode:
          type: string
          description: Code that indicates whether the next transfer amount is fixed or variable.
          enum:
          - Fixed
          - Variable
          - IntPlusDistribution
          - BalanceMinusDistribution
        ACHEntryClass:
          type: string
          description: |-
            Code used to identify the NACHA payment type (standard entry class).

            **Note:** Refers to ACH standard entry class.
          enum:
          - PPD
          - CCD
          - WEB
          - CIE
        NoticeData:
          type: array
          description: |+
            Details used for notice generation each time when a transfer takes place.

          items:
            $ref: '#/components/schemas/XferModOperRequest_XferInfo_ExtendedXferData_NoticeData'
        ForcePostInd:
          type: boolean
          description: |-
            Flag to indicate whether to process a transfer using the force pay transaction code.

            **Note:** Refers to force payment.
        FeeIdent:
          maxLength: 36
          type: string
          description: |-
            Identification code used to identify fee applicable for a transfer.

            **Note:** The transfer charge code identifies one of 999 transfer fee codes in DDA fee specifications.
        PostingPriority:
          type: string
          description: "Code to indicate the posting sequence of transfer in relation to other transactions.\n\n**Note:** Refers to priority code and indicates the posting sequence of a recurring transfer in relation to other (check) transactions. "
          enum:
          - First
          - Last
      description: Details of additional information used to add instructions for the fund transfer.
    XferModOperRequest_XferInfo_RefData:
      required:
      - RefType
      properties:
        RefType:
          type: string
          description: "Type of reference information used.\n\n**Note:** XferDesc refers to the description of the transfer. The description of the transfers is printed on reports, statement inquiries and customer notification.\n\n\n\nPayorDesc refers to the payor identification. \n\n\n\nPayeeDesc refers to a vendor number and only required if external transfer account type is Vendor(BPM)."
          enum:
          - PayorDesc
          - XferDesc
          - PayeeDesc
          - TranCode
        RefDesc:
          maxLength: 1024
          type: string
          description: Description of the identifier.
    XferModOperRequest_XferInfo:
      type: object
      properties:
        FromAcctRef:
          $ref: '#/components/schemas/XferModOperRequest_XferInfo_FromAcctRef'
        ToAcctRef:
          $ref: '#/components/schemas/XferModOperRequest_XferInfo_ToAcctRef'
        CurAmt:
          $ref: '#/components/schemas/XferModOperRequest_XferInfo_CurAmt'
        RecurModel:
          $ref: '#/components/schemas/XferModOperRequest_XferInfo_RecurModel'
        RelationshipMgr:
          type: array
          description: Information about the financial institution officers who have management-related responsibilities of the account.
          items:
            $ref: '#/components/schemas/XferModOperRequest_XferInfo_RelationshipMgr'
        ReportGroupCode:
          type: string
          description: "Client-defined code to group the accounts for reporting purposes.\n\n\n\n**Note:** Refers to the client-defined miscellaneous code that is used to group portfolios primarily as selection criteria for reporting. Values are 000-999. "
        ExtendedXferData:
          $ref: '#/components/schemas/XferModOperRequest_XferInfo_ExtendedXferData'
        RefData:
          type: array
          description: Details of reference information used to correlate to transactions provided in bank statements.
          items:
            $ref: '#/components/schemas/XferModOperRequest_XferInfo_RefData'
        LastXferDt:
          pattern: '[0-9]{4}-[0-9]{2}-[0-9]{2}(T[0-9]{2}:[0-9]{2}(:[0-9]{2}(\.[0-9]{3})?((-|\+)[0-9]{2}:[0-9]{2})?)?)?'
          type: string
          description: |-
            Date and time when the last transfer of funds occurred.

            Format: MM/DD/YYYY

            Example: 01/31/2021
        LastXferAmt:
          type: object
          description: Details of last transfer amount.
      description: Details of an automatic fund transfer.
    XferModOperResponse_XferStatusRec_XferKeys:
      required:
      - AcctKeys
      type: object
      properties:
        AcctKeys:
          $ref: '#/components/schemas/SecuredXferDelOperRequest_XferKeys_AcctKeys'
        XferIdent:
          maxLength: 36
          type: string
          description: |-
            Unique identifier of a transfer record.

            **Note:** Refers to the addenda number.
      description: Unique information to identify the transaction record.
    XferModOperResponse_XferStatusRec:
      required:
      - XferKeys
      - XferStatus
      type: object
      properties:
        XferKeys:
          $ref: '#/components/schemas/XferModOperResponse_XferStatusRec_XferKeys'
        XferStatus:
          $ref: '#/components/schemas/SecuredXferDelOperResponse_XferStatusRec_XferStatus'
      description: Fund transfer status details used in a response message to return results from an add, modify or delete operation.
