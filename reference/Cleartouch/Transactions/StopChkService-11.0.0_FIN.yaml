---
openapi: 3.0.1
info:
  title: Stop Check Service
  description: "Stop check is a request made to a financial institution to cancel a check or payment that is not being processed yet. <br>\n    The stop check service manages stop-check payments associated with the following product accounts: \n      -  Demand Deposit Account (DDA)\n      -  Saving Deposit Account (SDA)\n      -  Certificate of Deposit Account (CDA)\n      -  Loan Account (LOAN)"
  version: 11.0.0_FIN
servers:
- url: /
paths:
  /stopchkservice/stophold/stopChecks:
    put:
      tags:
      - Update Stop Check
      summary: Update Stop Check
      description: "Update Stop Check API modifies a stop check payment request.<br>Client application should use this API to modify a stop check payment which has been recently added (that is to change stop check payment which is created few minutes later) or a request that has not started yet. A better approach to modify the stop check payment is to cancel the existing stop and place a new stop check payment. Otherwise, if the original stop was applied already, it would be hard to research on issues.<br><br>To download the API Specifications and Postman Collection, click <a href=\"../docs/?path=docs/resources/resources.md\" title=\"Click to open\">Resources</a>."
      operationId: stopChkMod
      parameters:
      - name: EFXHeader
        in: header
        description: EFX Header is a common and required component for all API calls, it contains client-related parameters. Please note that the header parameters should be sent as a JSON object under the EFXHeader parameter. To view the list of header parameters, <a href="../docs?path=docs/api-ref-EFX-header.md" title="Click to open"> click here.</a>
        required: true
        schema:
          $ref: '#/components/schemas/EFXHeader'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/stopChkModRequest'
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/stopChkModResponse'
        default:
          description: To view the complete list of response codes, <a href="../docs/?path=docs/response-codes.md" title="Click to open">click here.</a>
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/stopChkModResponse'
      x-group-name: Stop Check Service
      x-proxy-name: Update Stop Check
      x-child-product-name: Transactions
      x-core: Finxact
    post:
      tags:
      - Add Stop Check
      summary: Add Stop Check
      description: "Add Stop Check API creates a request to stop (refuse) check payments where the check is still unpaid.\n        <br>\n        To download the API Specifications and Postman Collection, click <a href=\"../docs/?path=docs/resources/resources.md\" title=\"Click to open\">Resources</a>."
      operationId: stopChkAdd
      parameters:
      - name: EFXHeader
        in: header
        description: EFX Header is a common and required component for all API calls, it contains client-related parameters. Please note that the header parameters should be sent as a JSON object under the EFXHeader parameter. To view the list of header parameters, <a href="../docs?path=docs/api-ref-EFX-header.md" title="Click to open"> click here.</a>
        required: true
        schema:
          $ref: '#/components/schemas/EFXHeader'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/stopChkAddRequest'
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/stopChkAddResponse'
        default:
          description: To view the complete list of response codes, <a href="../docs/?path=docs/response-codes.md" title="Click to open">click here.</a>
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/stopChkAddResponse'
      x-group-name: Stop Check Service
      x-proxy-name: Add Stop Check
      x-child-product-name: Transactions
      x-core: Finxact
  /stopchkservice/stophold/stopChecks/secured:
    put:
      tags:
      - Cancel Stop Check
      summary: Cancel Stop Check
      description: "Cancel Stop Check API cancels a stop that was placed on a check payments.\n        <br>\n        To download the API Specifications and Postman Collection, click <a href=\"../docs/?path=docs/resources/resources.md\" title=\"Click to open\">Resources</a>."
      operationId: stopChkCanSecured
      parameters:
      - name: EFXHeader
        in: header
        description: EFX Header is a common and required component for all API calls, it contains client-related parameters. Please note that the header parameters should be sent as a JSON object under the EFXHeader parameter. To view the list of header parameters, <a href="../docs?path=docs/api-ref-EFX-header.md" title="Click to open"> click here.</a>
        required: true
        schema:
          $ref: '#/components/schemas/EFXHeader'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/stopChkCanSecuredRequest'
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/stopChkCanSecuredResponse'
        default:
          description: To view the complete list of response codes, <a href="../docs/?path=docs/response-codes.md" title="Click to open">click here.</a>
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/stopChkCanSecuredResponse'
      x-group-name: Stop Check Service
      x-proxy-name: Cancel Stop Check
      x-child-product-name: Transactions
      x-core: Finxact
    post:
      tags:
      - Get Stop Check
      summary: Get Stop Check.
      description: "Get Stop Check API retrieves a single or list of stops placed on check payments against the account.\n        <br>\n        To download the API Specifications and Postman Collection, click <a href=\"../docs/?path=docs/resources/resources.md\" title=\"Click to open\">Resources</a>."
      operationId: stopCheckInqSecured
      parameters:
      - name: EFXHeader
        in: header
        description: EFX Header is a common and required component for all API calls, it contains client-related parameters. Please note that the header parameters should be sent as a JSON object under the EFXHeader parameter. To view the list of header parameters, <a href="../docs?path=docs/api-ref-EFX-header.md" title="Click to open"> click here.</a>
        required: true
        schema:
          $ref: '#/components/schemas/EFXHeader'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/stopCheckInqSecuredRequest'
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/stopCheckInqSecuredResponse'
        default:
          description: To view the complete list of response codes, <a href="../docs/?path=docs/response-codes.md" title="Click to open">click here.</a>
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/stopCheckInqSecuredResponse'
      x-group-name: Stop Check Service
      x-proxy-name: Get Stop Check
      x-child-product-name: Transactions
      x-core: Finxact
components:
  schemas:
    stopChkAddRequest:
      required:
      - StopChkInfo
      properties:
        StopChkInfo:
          $ref: '#/components/schemas/stopChkAddRequest_StopChkInfo'
    stopChkAddResponse:
      required:
      - StopChkStatusRec
      properties:
        Status:
          $ref: '#/components/schemas/StatusType'
        StopChkStatusRec:
          $ref: '#/components/schemas/stopChkAddResponse_StopChkStatusRec'
    stopChkCanSecuredRequest:
      required:
      - StopChkKeys
      properties:
        StopChkKeys:
          $ref: '#/components/schemas/stopChkCanSecuredRequest_StopChkKeys'
    stopChkCanSecuredResponse:
      required:
      - StopChkStatusRec
      properties:
        Status:
          $ref: '#/components/schemas/StatusType'
        StopChkStatusRec:
          $ref: '#/components/schemas/stopChkCanSecuredResponse_StopChkStatusRec'
    stopCheckInqSecuredRequest:
      required:
      - StopChkSel
      properties:
        RecCtrlIn:
          $ref: '#/components/schemas/stopCheckInqSecuredRequest_RecCtrlIn'
        StopChkSel:
          $ref: '#/components/schemas/stopCheckInqSecuredRequest_StopChkSel'
    stopCheckInqSecuredResponse:
      required:
      - StopChkRec
      properties:
        Status:
          $ref: '#/components/schemas/StatusType'
        RecCtrlOut:
          $ref: '#/components/schemas/stopCheckInqSecuredResponse_RecCtrlOut'
        StopChkRec:
          type: array
          description: Details of the stop check record.
          items:
            $ref: '#/components/schemas/stopCheckInqSecuredResponse_StopChkRec'
    stopChkModRequest:
      required:
      - StopChkInfo
      - StopChkKeys
      properties:
        StopChkKeys:
          $ref: '#/components/schemas/stopChkModRequest_StopChkKeys'
        StopChkInfo:
          $ref: '#/components/schemas/stopChkModRequest_StopChkInfo'
    stopChkModResponse:
      required:
      - StopChkStatusRec
      properties:
        Status:
          $ref: '#/components/schemas/StatusType'
        StopChkStatusRec:
          $ref: '#/components/schemas/stopChkAddResponse_StopChkStatusRec'
    EFXHeader:
      required:
      - OrganizationId
      type: string
      properties:
        OrganizationId:
          maxLength: 36
          type: string
          description: Organization Identifier is a unique identifier that represents the financial institution or holding company.  This identifier does not necessarily match the organization identifier or bank identifier in the backend system. To ensure uniqueness across all institutions in all parts of the world, use the Dun & Bradstreet number.
        TrnId:
          maxLength: 36
          type: string
          description: Transaction UUID (Universal Unique Identifier) of the current message.
        VendorId:
          maxLength: 255
          type: string
          description: Unique vendor identification number provided by Fiserv to identify the vendor.
        Context:
          $ref: '#/components/schemas/Context'
      example: '{ "TrnId": "622182", "OrganizationId": "PRMOrg10", "VendorId": "112233", "Context":{ "Channel": "WEB" } }'
    Context:
      type: object
      properties:
        ClientAppName:
          maxLength: 40
          type: string
          description: Name of the client application that is used to send the service request, such as Architect, Commercial Center and others.
        Channel:
          maxLength: 80
          type: string
          description: Name of the channel used by the client application. * 'Online' * 'Phone'  * 'Branch' * 'EFT'
        TrnIdent:
          maxLength: 36
          type: string
          description: Unique identification value of transaction defined by the client.  Also known as Trace Number or Sequence in some systems. When used by a Network defined transaction,  the value of this identifier is typically set to narrow character of 12.
        ClientDateTime:
          pattern: '[0-9]{4}-[0-9]{2}-[0-9]{2}(T[0-9]{2}:[0-9]{2}(:[0-9]{2}(\.[0-9]{3})?((-|\+)[0-9]{2}:[0-9]{2})?)?)?'
          type: string
          description: Client date and time as reported by the client application that is making the service request.
        ClientTerminalSeqNum:
          maxLength: 36
          description: 'Client terminal sequence number. type: string'
        OriginatorType:
          type: integer
          description: "The type of organization that the originator represents. This is usually a coded value representing the industry that the organization operates in. It contains the SIC code. For ISO 8583 (DE18) the element is restricted to narrow character of maximum length of 4.  Originator Type codes that apply to financialinstitutions are:\n  6010 - Financial institution—bank, savings and loan (S and L), or credit union  \n  6011 - Financial institution—ATM \n  6012 - Financial institution—merchandise sale \n  6999 - Financial institution—home banking"
          format: int64
        TerminalIdent:
          maxLength: 36
          type: string
          description: Unique code identifying a terminal at the card acceptor location (such as terminal code or terminal number of ATM). See For ISO 8583 (DE41) the element is restricted to a narrow character with maximum length of 8.
        TerminalAddr1:
          maxLength: 64
          type: string
          description: Terminal address line 1.
        TerminalAddr2:
          maxLength: 64
          type: string
          description: Terminal address line 2.
        TerminalAddr3:
          maxLength: 64
          type: string
          description: Terminal address line 3.
        TerminalAddr4:
          maxLength: 64
          type: string
          description: Terminal address line 4.
        TerminalCity:
          maxLength: 32
          type: string
          description: Terminal city name.
        TerminalCounty:
          maxLength: 32
          type: string
          description: Terminal county name.
        TerminalStateProv:
          maxLength: 32
          type: string
          description: State province value as per ISO 3166-2:US codes where the terminal is located.
        TerminalPostalCode:
          maxLength: 11
          type: string
          description: 'Postal Code where the terminal is located. '
        TerminalCountryCodeSource:
          maxLength: 80
          type: string
          description: |-
            Standard country code format where the terminal is located. Valid values are:
              * 'ISO3166-Numeric'
              * 'ISO3166-Alpha-3'
        TerminalCountryCodeValue:
          maxLength: 20
          type: string
          description: Country code value as per the ISO source code set in the TerminalCountryCodeSource field.
        PhoneType:
          maxLength: 80
          type: string
          description: 'Type of a phone. Valid values are: * ''EvePhone'' * ''DayPhone'' * ''EveFax'' * ''DayFax'' * ''Home'' * ''Work'' * ''Mobile'' * ''Fax'' * ''Pager'' * ''Modem'' * ''Other'''
        PhoneNum:
          type: string
          description: Phone number.
        PhoneExchange:
          maxLength: 80
          type: string
          description: Phone exchange.
        TerminalOwnerName:
          maxLength: 40
          type: string
          description: Name of the owner or operator of the terminal.  For ISO 8583 (DE43) the element is restricted to C-15.
        SystTraceAuditNum:
          maxLength: 36
          type: string
          description: Number assigned by the transaction originator to assist in identifying a transaction uniquely. The trace number remains unchanged for all messages throughout the life of a transaction. This number is not a terminal receipt number. The originating processor increments the trace number by one for each transaction sent to the switch. For ISO 8583 (DE11) the element is restricted to NC-6
        NetworkIdent:
          maxLength: 80
          type: string
          description: Identifies the interchange network for the transaction. The transaction is applied to the specified network settlement counts and balances. For ISO 8583 (DE62) the element is restricted to NC-6
        NetworkRefIdent:
          maxLength: 36
          type: string
          description: Document reference supplied by the system retaining the original source document and used to assist in locating that document. The acquirer of a transaction assigns this number. The issuer processor must retain it in the event that a chargeback is submitted for the transaction. For ISO 8583 (DE37) the element is restricted to C-12
        AcquirerIdent:
          maxLength: 36
          type: string
          description: Identifier of the acquirer who processes the financial transaction. It is a mandatory element in all authorization and financial messages and does not change throughout the life of a transaction. EPOC considers the acquirer as the terminal owner for reporting purposes. For ISO 8583 (DE32) the element is restricted to C-12
        AcquirerCountryCodeSource:
          maxLength: 80
          type: string
          description: |-
            Standard country code format where the acquirer is located. Valid values are:
              * 'ISO3166-Numeric'
              * 'ISO3166-Alpha-3'
        AcquirerCountryCodeValue:
          maxLength: 20
          type: string
          description: Country code value of Acquirer as per the ISO source code set in the AcquirerCountryCodeSource field.
        MerchNum:
          maxLength: 23
          type: string
          description: Unique identification number of a merchant.  For ISO 8583 (DE42) the element is restricted to C-15
        SettlementDate:
          type: string
          description: Transaction settlement date. Used by ISO 8583 (DE63).
          format: date
        SettlementIdent:
          maxLength: 36
          type: string
          description: Identification of the settlement account in which the transaction will be settled.
        BusinessApplIdent:
          maxLength: 80
          type: string
          description: |-
            Identification of the business application. Valid values are:
              * 'P2P' - Person-to-Person
              * 'C2B' - Consumer-to-Business
              * 'A2A' - Account-to-Account
              * 'B2C' - Business-to-Consumer
              * 'B2B' - Business-to-Business
              * 'G2C' - Government-to-Consumer
              * 'C2G' - Consumer-to-Government
        BranchIdent:
          maxLength: 22
          type: string
          description: Branch identification number.
        TellerIdent:
          maxLength: 80
          type: string
          description: Teller identification number.
        TillIdent:
          maxLength: 80
          type: string
          description: Till identification number.
        AMPMCode:
          type: string
          description: 'Transaction posting code. '
          enum:
          - AM
          - PM
        ReentryType:
          type: string
          description: Type of re-entry.
          enum:
          - Manual
          - Auto
        AdditionalSettings:
          type: object
          additionalProperties: true
          description: Contains additional information required to successfully process the transaction. Required By Cleartouch
    StatusType:
      title: StatusType
      required:
      - Severity
      - StatusCode
      - StatusDesc
      - SvcProviderName
      type: object
      properties:
        Id:
          type: string
          description: Status identification number.
        StatusCode:
          maxLength: 20
          type: string
          description: EFX Standard Status code that indicates the result of API response. Refer to the <a href="../docs?path=docs/response-codes.md">Response Codes</a> section to view the list of all possible status codes.
        StatusDesc:
          maxLength: 255
          type: string
          description: Brief information about the EFX status code. Refer to the <a href="../docs?path=docs/response-codes.md">Response Codes</a> section for more information.
        Severity:
          $ref: '#/components/schemas/SeverityType'
        SvcProviderName:
          maxLength: 1024
          type: string
          description: |-
            Name of a service provider. Possible values are:
              -  Cleartouch
              -  DNA
              -  Precision
              -  Premier
              -  Signature
        ServerStatusCode:
          maxLength: 20
          type: string
          description: Server status code of the service provider's application.
        ServerStatusDesc:
          maxLength: 255
          type: string
          description: Description of the server status code of the service provider's application.
        OvrdExceptionInd:
          type: boolean
          description: Flag to indicate whether the exception can be overridden by resubmitting the request message.
        SubjectRole:
          type: string
          description: Authorization level required to override, such as Teller and Supervisor.
        SubjectElement:
          type: array
          description: Information about the elements that caused the status.
          items:
            $ref: '#/components/schemas/SubjectElementType'
        ContentHTML:
          type: string
          description: Response status in HTML format. This parameter returns only in some cases.
        AdditionalStatus:
          type: array
          description: Details of the additional status of the response message. This aggregate can appear once for each additional status that the server is expected to provide.
          items:
            $ref: '#/components/schemas/AdditionalStatusType'
      description: Details of the API response messages.
    SeverityType:
      title: SeverityType
      type: string
      description: Severity type of the EFX status code. Refer to the <a href="../docs?path=docs/response-codes.md">Response Codes</a> section for more information.
      enum:
      - Error
      - Warning
      - Info
    SubjectElementType:
      title: SubjectElementType
      type: object
      properties:
        Path:
          type: string
          description: Path of the element (in XPath absolute notation format) where the error occurred.  If the error occurred at the top-level element, then name of the element is returned in the response.
        ServerPath:
          maxLength: 256
          type: string
          description: Server Path is the Service Provider’s identification of the field in their schema, such as an XPath, field ID, or field name that is associated with the ServerStatusCode. If this element is set, it must be returned in the ServerPath element of the OvrdExceptionData aggregate if the exception is being overridden.
        Value:
          maxLength: 256
          type: string
          description: Human readable information of the EFX data value of the element to be overridden. This value should not be provided for the fields that cannot be represented as a string, for example, binary data.
      description: Information about the subject element
    AdditionalStatusType:
      title: AdditionalStatusType
      type: object
      properties:
        StatusCode:
          maxLength: 20
          type: string
          description: EFX Standard Status code that indicates the result of API response. Refer to the <a href="../docs?path=docs/response-codes.md">Response Codes</a> section to view the list of all possible status codes.
        StatusDesc:
          maxLength: 255
          type: string
          description: Brief information about the EFX status code. Refer to the <a href="../docs?path=docs/response-codes.md">Response Codes</a> for more information.
        Severity:
          $ref: '#/components/schemas/SeverityType'
        SvcProviderName:
          maxLength: 1024
          type: string
          description: |-
            Name of a service provider. Possible values are:
              - Cleartouch
              - DNA
              - Precision
              - Premier
              - Signature
        ServerStatusCode:
          maxLength: 20
          type: string
          description: 'Server status code of the service provider''s application.  '
        ServerStatusDesc:
          maxLength: 255
          type: string
          description: Description of the server status code of the service provider's application.
        OvrdExceptionInd:
          type: boolean
          description: Flag that indicates whether the exception can be overridden by resubmitting the request message.
        SubjectElement:
          type: array
          description: Information about the elements that caused the status.
          items:
            $ref: '#/components/schemas/SubjectElementType'
    stopChkAddRequest_StopChkInfo_AcctKeys:
      required:
      - AcctId
      type: object
      properties:
        AcctId:
          maxLength: 36
          type: string
          description: |+
            Unique identification number of an account record.





        AcctType:
          type: string
          description: |-
            Type of an account.



            For more information about account types, refer to the Account Types section in <a href="../docs?path=docs/api-glossary.md"  title="Click to open"> Glossary</a>.

            **Note:** The following values indicate:

            - DDA: Demand deposit account

            - MMA: Money market account

            - SDA: Savings deposit account

            - CDA: Certificate of deposit

            - CMA: Cash management account
          enum:
          - DDA
          - MMA
          - SDA
          - CDA
          - CMA
      description: Unique information to identify the account that is a subject of the stop check.
    stopChkAddRequest_StopChkInfo_ChkNumRange:
      type: object
      properties:
        ChkNumStart:
          maxLength: 22
          type: string
          description: |+
            Starting number of the check number range used for the stop check record.

        ChkNumEnd:
          maxLength: 22
          type: string
          description: |+
            Ending number of the check number range used for the stop check record.

      description: Details of the check range that includes the start check number (ChkNumStart) and the end check number (ChkNumEnd) information for the stop check transaction.
    stopChkAddRequest_StopChkInfo_CurAmt_CurCode:
      required:
      - CurCodeValue
      type: object
      properties:
        CurCodeType:
          type: string
          description: "Standard ISO currency code format. \n\n\n\nRefer to the <a href=\"https://en.wikipedia.org/wiki/ISO_4217\">ISO 4217 codes </a> to view the list of all currency codes."
          enum:
          - ISO4217-Alpha
        CurCodeValue:
          type: string
          description: "Currency code value as per the ISO currency code format set in the CurCodeType field.\n\nFor example, currency code for the US Dollar in alpha and numeric formats are:\n\n- USD (in ISO4217-Alpha format) \n\n- 840 (in ISO4217-Numeric format)\n\n\n\nPresently only USD currency code is supported.\n\n"
      description: Details of currency code.
    stopChkAddRequest_StopChkInfo_CurAmt:
      type: object
      properties:
        Amt:
          type: number
          description: Amount in decimal format.
        CurCode:
          $ref: '#/components/schemas/stopChkAddRequest_StopChkInfo_CurAmt_CurCode'
      description: |+
        Details of the currency amount.

        **Note:** Only a single check amount or a range of amounts should be provided to create a stop check record.

    stopChkAddRequest_StopChkInfo_CurAmtRange_LowCurAmt:
      type: object
      properties:
        Amt:
          type: number
          description: Amount in decimal format.
        CurCode:
          $ref: '#/components/schemas/stopChkAddRequest_StopChkInfo_CurAmt_CurCode'
      description: Details of the minimum amount range that contains the amount value and currency code information.
    stopChkAddRequest_StopChkInfo_CurAmtRange_HighCurAmt:
      type: object
      properties:
        Amt:
          type: number
          description: Amount in decimal format.
        CurCode:
          $ref: '#/components/schemas/stopChkAddRequest_StopChkInfo_CurAmt_CurCode'
      description: Details of the maximum amount range that contains the amount value and currency code information.
    stopChkAddRequest_StopChkInfo_CurAmtRange:
      type: object
      properties:
        LowCurAmt:
          $ref: '#/components/schemas/stopChkAddRequest_StopChkInfo_CurAmtRange_LowCurAmt'
        HighCurAmt:
          $ref: '#/components/schemas/stopChkAddRequest_StopChkInfo_CurAmtRange_HighCurAmt'
      description: |+
        Details of the amount range that includes the minimum and maximum currency amount to consider for the stop check transaction.

        **Note:** Only a single check amount or a range of amounts should be provided to create a stop check record.

    stopChkAddRequest_StopChkInfo_TranFee_FeeAmt:
      type: object
      properties:
        Amt:
          type: number
          description: Amount in decimal format.
        CurCode:
          $ref: '#/components/schemas/stopChkAddRequest_StopChkInfo_CurAmt_CurCode'
      description: Details of the stop check transaction processing fee amount.
    stopChkAddRequest_StopChkInfo_TranFee_BillingInstruction_FromAcctKeys:
      required:
      - AcctId
      type: object
      properties:
        AcctId:
          maxLength: 36
          type: string
          description: |+
            Unique identification number of an account record.





        AcctType:
          type: string
          description: |-
            Type of an account.



            For more information about account types, refer to the Account Types section in <a href="../docs?path=docs/api-glossary.md"  title="Click to open"> Glossary</a>.

            **Note:** The following values indicate:

            - DDA: Demand deposit account

            - MMA: Money market account

            - SDA: Savings deposit account

            - CDA: Certificate of deposit

            - CMA: Cash management account
          enum:
          - DDA
          - MMA
          - SDA
          - CDA
          - CMA
      description: Unique information to identify from account record.
    stopChkAddRequest_StopChkInfo_TranFee_BillingInstruction:
      required:
      - BillingMethod
      type: object
      properties:
        BillingMethod:
          type: string
          description: |+
            Type of billing.

          enum:
          - DirectDebit
        FromAcctKeys:
          $ref: '#/components/schemas/stopChkAddRequest_StopChkInfo_TranFee_BillingInstruction_FromAcctKeys'
      description: Details of the billing instruction used to specify how to charge for the stop check transaction fee.
    stopChkAddRequest_StopChkInfo_TranFee:
      type: object
      properties:
        FeeIdent:
          maxLength: 36
          type: string
          description: |-
            Unique identification number to identify the fee amount.

            **Note:** If this value is provided, then the fee amount information is not used to determine the fee.
        FeeAmt:
          $ref: '#/components/schemas/stopChkAddRequest_StopChkInfo_TranFee_FeeAmt'
        BillingInstruction:
          $ref: '#/components/schemas/stopChkAddRequest_StopChkInfo_TranFee_BillingInstruction'
      description: Information about billing details and transaction fee for a stop check transaction.
    stopChkAddRequest_StopChkInfo:
      type: object
      properties:
        AcctKeys:
          $ref: '#/components/schemas/stopChkAddRequest_StopChkInfo_AcctKeys'
        ChkNumRange:
          $ref: '#/components/schemas/stopChkAddRequest_StopChkInfo_ChkNumRange'
        Name:
          maxLength: 80
          type: string
          description: Name of the payee.
        CurAmt:
          $ref: '#/components/schemas/stopChkAddRequest_StopChkInfo_CurAmt'
        VariancePct:
          type: number
          description: "Variance percentage by which the amount of an item may vary from the amount specified in the check amount field and still be considered as a stop or suspect. \n\n**Note:** Request should not include variance percentage if the CurAmtRange describes an amount variance."
        CurAmtRange:
          $ref: '#/components/schemas/stopChkAddRequest_StopChkInfo_CurAmtRange'
        OrigDt:
          type: string
          description: "Date on which the customer originates the check.\n\n\n\nFormat: yyyy-mm-dd \n\nExample: 2021-11-20 "
          format: date
        StopChkDt:
          type: string
          description: "Date on which the stop check request is initiated.\n\n\n\nFormat: yyyy-mm-dd \n\nExample: 2021-11-20 "
          format: date
        ExpDt:
          type: string
          description: "Date after which stop check is scheduled to expire.\n\n\n\nFormat: yyyy-mm-dd \n\nExample: 2021-11-20\n\n**Note:** If an expiration date is not provided, then the stop check does not expire."
          format: date
        StopChkReasonCode:
          type: string
          description: "Code to identify the reason for a stop check placed.\n\n**Note:** The following values are considered:\n\n- Lost\n\n- Stolen\n\n- Dispute\n\n- Fraud\n\n- CustomerRequest\n\n- BankRequest\n\n- Other "
          enum:
          - Lost
          - Stolen
          - Dispute
          - Fraud
          - CustomerRequest
          - BankRequest
          - Other
        Desc:
          maxLength: 100
          type: string
          description: Description that specifies the reason for the stop check.
        TranFee:
          $ref: '#/components/schemas/stopChkAddRequest_StopChkInfo_TranFee'
        CardSignedInd:
          type: boolean
          description: Flag to indicate the customer has signed a stop payment card.
      description: Information about the client-managed properties of the stop check record.
    stopChkAddResponse_StopChkStatusRec_StopChkKeys_AcctKeys:
      required:
      - AcctId
      type: object
      properties:
        AcctId:
          maxLength: 36
          type: string
          description: |+
            Unique identification number of an account record.





        AcctType:
          type: string
          description: |-
            Type of an account.



            For more information about account types, refer to the Account Types section in <a href="../docs?path=docs/api-glossary.md"  title="Click to open"> Glossary</a>.

            **Note:** The following values indicate:

            - DDA: Demand deposit account

            - MMA: Money market account

            - SDA: Savings deposit account

            - CDA: Certificate of deposit

            - CMA: Cash management account
          enum:
          - DDA
          - MMA
          - SDA
          - CDA
          - CMA
      description: |-
        Unique information to identify the account that is a subject of the stop check.

        **Note:** Composition of the key includes AcctKeys when KeyCompatiblity_Rule is "true" for the ClientAppName.
    stopChkAddResponse_StopChkStatusRec_StopChkKeys:
      required:
      - AcctKeys
      type: object
      properties:
        StopChkId:
          maxLength: 36
          type: string
          description: |-
            Unique identification number to identify the stop check.

            **Note:** Refers to the native key.
        AcctKeys:
          $ref: '#/components/schemas/stopChkAddResponse_StopChkStatusRec_StopChkKeys_AcctKeys'
        StopChkIdent:
          maxLength: 36
          type: string
          description: |-
            Unique identification code used in combination with account number and account type to identify the stop check record.

            **Note:** Composition of the key includes AcctKeys when KeyCompatiblity_Rule is "true" for the ClientAppName.
      description: |-
        Unique information to identify a stop check record.

        **Note:** Key may vary depending on the configuration. The key can be expressed as a native key (StopChkId) or as a compatibility key (AcctKeys and StopChkIdent).
    stopChkAddResponse_StopChkStatusRec_StopChkStatus:
      required:
      - StopChkStatusCode
      type: object
      properties:
        StopChkStatusCode:
          type: string
          description: Code to indicate the status for the stop check record.
          enum:
          - Valid
        EffDt:
          pattern: '[0-9]{4}-[0-9]{2}-[0-9]{2}(T[0-9]{2}:[0-9]{2}(:[0-9]{2}(\.[0-9]{3})?((-|\+)[0-9]{2}:[0-9]{2})?)?)?'
          type: string
          description: "Effective date and time when the stop check status is changed. \n\nFormat: yyyy-MM-ddTHH:mm:ss.SSS\n\nExample: 2021-01-31T23:59:59.999"
      description: Details of the stop check status.
    stopChkAddResponse_StopChkStatusRec:
      required:
      - StopChkKeys
      - StopChkStatus
      type: object
      properties:
        StopChkKeys:
          $ref: '#/components/schemas/stopChkAddResponse_StopChkStatusRec_StopChkKeys'
        StopChkStatus:
          $ref: '#/components/schemas/stopChkAddResponse_StopChkStatusRec_StopChkStatus'
      description: Details of the stop check status record in response to modify stop check record.
    stopChkCanSecuredRequest_StopChkKeys_AcctKeys:
      required:
      - AcctId
      type: object
      properties:
        AcctId:
          maxLength: 36
          type: string
          description: |+
            Unique identification number of an account record.





        AcctType:
          type: string
          description: |-
            Type of an account.



            For more information about account types, refer to the Account Types section in <a href="../docs?path=docs/api-glossary.md"  title="Click to open"> Glossary</a>.

            **Note:** The following values indicate:

            - DDA: Demand deposit account

            - MMA: Money market account

            - SDA: Savings deposit account

            - CDA: Certificate of deposit

            - CMA: Cash management account
          enum:
          - DDA
          - MMA
          - SDA
          - CDA
          - CMA
      description: |-
        Unique information to identify the account that is a subject of the stop check.

        **Note:** Client application uses AcctKey with the compatibility key.
    stopChkCanSecuredRequest_StopChkKeys:
      required:
      - AcctKeys
      type: object
      properties:
        StopChkId:
          maxLength: 36
          type: string
          description: |-
            Unique identification number to identify the stop check.

            **Note:** Refers to the native key.
        AcctKeys:
          $ref: '#/components/schemas/stopChkCanSecuredRequest_StopChkKeys_AcctKeys'
        StopChkIdent:
          maxLength: 36
          type: string
          description: |-
            Unique identification code used in combination with account number and account type to identify the stop check record.

            **Note:** Client application uses StopChkIdent with the compatibility key.
      description: |-
        Unique information to identify a stop check record.

        **Note:** Key may vary depending on the configuration. The key can be expressed as a native key (StopChkId) or as a compatibility key (AcctKeys and StopChkIdent).
    stopChkCanSecuredResponse_StopChkStatusRec_StopChkKeys:
      required:
      - AcctKeys
      type: object
      properties:
        StopChkId:
          maxLength: 36
          type: string
          description: Unique identification number to identify the stop check.
        AcctKeys:
          $ref: '#/components/schemas/stopChkAddRequest_StopChkInfo_AcctKeys'
        StopChkIdent:
          maxLength: 36
          type: string
          description: Unique identification code used in combination with account number and account type to identify the stop check record.
      description: Unique information to identify a stop check record.
    stopChkCanSecuredResponse_StopChkStatusRec_StopChkStatus:
      required:
      - StopChkStatusCode
      type: object
      properties:
        StopChkStatusCode:
          type: string
          description: Code to indicate the status for the stop check record.
          enum:
          - Deleted
        EffDt:
          pattern: '[0-9]{4}-[0-9]{2}-[0-9]{2}(T[0-9]{2}:[0-9]{2}(:[0-9]{2}(\.[0-9]{3})?((-|\+)[0-9]{2}:[0-9]{2})?)?)?'
          type: string
          description: "Effective date and time when the stop check status is changed. \n\nFormat: yyyy-MM-ddTHH:mm:ss.SSS\n\nExample: 2021-01-31T23:59:59.999"
      description: Details of the stop check status.
    stopChkCanSecuredResponse_StopChkStatusRec:
      required:
      - StopChkKeys
      - StopChkStatus
      type: object
      properties:
        StopChkKeys:
          $ref: '#/components/schemas/stopChkCanSecuredResponse_StopChkStatusRec_StopChkKeys'
        StopChkStatus:
          $ref: '#/components/schemas/stopChkCanSecuredResponse_StopChkStatusRec_StopChkStatus'
      description: Details of the stop check status record.
    stopCheckInqSecuredRequest_RecCtrlIn:
      type: object
      properties:
        Cursor:
          maxLength: 250
          type: string
          description: 'Used in RecCtrlIn aggregate to request more records.  Next Record Pointer Included in the response only if additional records are available. '
        MaxRecLimit:
          type: integer
          description: 'Maximum number of records in the result set to satisfy the request. '
          format: int64
      description: Information retrieved for the criteria specified in the RecCtrlIn aggregate of the request message.
    stopCheckInqSecuredRequest_StopChkSel_AcctKeys:
      required:
      - AcctId
      type: object
      properties:
        AcctId:
          maxLength: 36
          type: string
          description: |+
            Unique identification number of an account record.





        AcctType:
          type: string
          description: |-
            Type of an account.



            For more information about account types, refer to the Account Types section in <a href="../docs?path=docs/api-glossary.md"  title="Click to open"> Glossary</a>.

            **Note:** The following values indicate:

            - DDA: Demand deposit account

            - MMA: Money market account

            - SDA: Savings deposit account

            - CDA: Certificate of deposit

            - CMA: Cash management account
          enum:
          - DDA
          - MMA
          - SDA
          - CDA
          - CMA
      description: |-
        Unique information to identify the account record.

        **Note:** The selection is done either by stop check keys (StopChkSel/StopChkKeys) or by other selection criteria. If selection is done by other selection criteria, then the StopItemSel/AcctKeys is required.
    stopCheckInqSecuredRequest_StopChkSel_ChkNumRange:
      type: object
      properties:
        ChkNumEnd:
          maxLength: 22
          type: string
          description: |+
            Ending number of the check number range used for the stop check record.

        ChkNumStart:
          maxLength: 22
          type: string
          description: |+
            Starting number of the check number range used for the stop check record.

      description: |-
        Details of the check range that includes the start check number (ChkNumStart) and the end check number (ChkNumEnd) information for the stop check transaction.

        **Note:** Provide either one check number or a range of checks. If both are provided, then check number range will take precedence.
    stopCheckInqSecuredRequest_StopChkSel_DtRange:
      type: object
      properties:
        EndDt:
          pattern: '[0-9]{4}-[0-9]{2}-[0-9]{2}(T[0-9]{2}:[0-9]{2}(:[0-9]{2}(\.[0-9]{3})?((-|\+)[0-9]{2}:[0-9]{2})?)?)?'
          type: string
          description: |-
            End date and time value.

            Format: yyyy-MM-ddTHH:mm:ss.SSS

            Example: 2021-01-31T23:59:59.999
        StartDt:
          pattern: '[0-9]{4}-[0-9]{2}-[0-9]{2}(T[0-9]{2}:[0-9]{2}(:[0-9]{2}(\.[0-9]{3})?((-|\+)[0-9]{2}:[0-9]{2})?)?)?'
          type: string
          description: |-
            Start date and time value.

            Format: yyyy-MM-ddTHH:mm:ss.SSS

            Example: 2021-01-31T23:59:59.999
      description: |-
        Details of the date range that includes the start date (StartDt) and the end date (EndDt) information.

        **Note:** This field is also used to identify the stops that started within the date range.
    stopCheckInqSecuredRequest_StopChkSel_StopChkKeys:
      required:
      - AcctKeys
      type: object
      properties:
        StopChkId:
          maxLength: 36
          type: string
          description: |+
            Unique identification number to identify the stop check.

            **Note:** Refers to the native key.





        AcctKeys:
          $ref: '#/components/schemas/stopChkAddResponse_StopChkStatusRec_StopChkKeys_AcctKeys'
        StopChkIdent:
          maxLength: 36
          type: string
          description: |-
            Unique identification code used in combination with account number and account type to identify the stop check record.

            **Note:** Composition of the key includes AcctKeys when KeyCompatiblity_Rule is "true" for the ClientAppName.



            If StopChkId is provided in the selection request, then it will take precedence over any other selection criteria.
      description: "Unique informatiomation to identify a stop check record. \n\n**Note:** Key may vary depending on the configuration. The key can be expressed as a native key (StopChkId) or as a compatibility key (AcctKeys and StopChkIdent)."
    stopCheckInqSecuredRequest_StopChkSel:
      type: object
      properties:
        AcctKeys:
          $ref: '#/components/schemas/stopCheckInqSecuredRequest_StopChkSel_AcctKeys'
        ChkNum:
          maxLength: 22
          type: string
          description: |-
            Number of the check used under a stop check process.

            **Note:** Provide either one check number or a range of checks. If both are provided, then check number range will take precedence.
        ChkNumRange:
          $ref: '#/components/schemas/stopCheckInqSecuredRequest_StopChkSel_ChkNumRange'
        DtRange:
          $ref: '#/components/schemas/stopCheckInqSecuredRequest_StopChkSel_DtRange'
        StopChkKeys:
          $ref: '#/components/schemas/stopCheckInqSecuredRequest_StopChkSel_StopChkKeys'
      description: Details of the client-specified selection criteria for stop check.
    stopCheckInqSecuredResponse_RecCtrlOut:
      required:
      - SentRecCount
      type: object
      properties:
        Cursor:
          maxLength: 250
          type: string
          description: 'Used in RecCtrlIn aggregate to request more records.  Next Record Pointer Included in the response only if additional records are available. '
        SentRecCount:
          type: integer
          description: Total number of records that matched the selection criteria and returned in the response message.
          format: int64
      description: Information retrieved for the criteria specified in the RecCtrlIn aggregate of the request message.
    stopCheckInqSecuredResponse_StopChkInfo_AcctKeys:
      required:
      - AcctId
      type: object
      properties:
        AcctId:
          maxLength: 36
          type: string
          description: |+
            Unique identification number of an account record.





        AcctType:
          type: string
          description: |-
            Type of an account.



            For more information about account types, refer to the Account Types section in <a href="../docs?path=docs/api-glossary.md"  title="Click to open"> Glossary</a>.

            **Note:** The following values indicate:

            - DDA: Demand deposit account

            - MMA: Money market account

            - SDA: Savings deposit account

            - CDA: Certificate of deposit

            - CMA: Cash management account
          enum:
          - DDA
          - MMA
          - SDA
          - CDA
          - CMA
      description: Unique information to identify the deposit account of the stop check payment.
    stopCheckInqSecuredResponse_StopChkInfo_ChkNumRange:
      type: object
      properties:
        ChkNumEnd:
          maxLength: 22
          type: string
          description: |+
            Ending number of the check number range used for the stop check record.

        ChkNumStart:
          maxLength: 22
          type: string
          description: |+
            Starting number of the check number range used for the stop check record.

      description: Details of the check range that includes the start check number (ChkNumStart) and the end check number (ChkNumEnd) information for the stop check transaction.
    stopCheckInqSecuredResponse_StopChkInfo_CurAmt:
      type: object
      properties:
        Amt:
          type: number
          description: Amount in decimal format.
        CurCode:
          $ref: '#/components/schemas/stopChkAddRequest_StopChkInfo_CurAmt_CurCode'
      description: Details of the currency amount.
    stopCheckInqSecuredResponse_StopChkInfo_CurAmtRange:
      type: object
      properties:
        HighCurAmt:
          $ref: '#/components/schemas/stopChkAddRequest_StopChkInfo_CurAmtRange_HighCurAmt'
        LowCurAmt:
          $ref: '#/components/schemas/stopChkAddRequest_StopChkInfo_CurAmtRange_LowCurAmt'
      description: Details of the amount range that includes the minimum and maximum currency amount to consider for the stop check transaction.
    stopCheckInqSecuredResponse_StopChkInfo_TranFee:
      type: object
      properties:
        FeeAmt:
          $ref: '#/components/schemas/stopChkAddRequest_StopChkInfo_TranFee_FeeAmt'
        FeeIdent:
          maxLength: 36
          type: string
          description: Unique identification number to identify the fee amount.
      description: Information about billing details and transaction fee for a stop check transaction.
    stopCheckInqSecuredResponse_StopChkInfo_VarianceAmt:
      type: object
      properties:
        Amt:
          type: number
          description: Amount in decimal format.
        CurCode:
          $ref: '#/components/schemas/stopChkAddRequest_StopChkInfo_CurAmt_CurCode'
      description: Variance amount is the amount by which the an item amount may vary from the amount specified in the check amount field, and still be considered to be a stop or suspect.
    stopCheckInqSecuredResponse_StopChkInfo:
      type: object
      properties:
        AcctKeys:
          $ref: '#/components/schemas/stopCheckInqSecuredResponse_StopChkInfo_AcctKeys'
        CardSignedInd:
          type: boolean
          description: Flag to indicate the customer has signed a stop payment card.
        ChkNumRange:
          $ref: '#/components/schemas/stopCheckInqSecuredResponse_StopChkInfo_ChkNumRange'
        CurAmt:
          $ref: '#/components/schemas/stopCheckInqSecuredResponse_StopChkInfo_CurAmt'
        CurAmtRange:
          $ref: '#/components/schemas/stopCheckInqSecuredResponse_StopChkInfo_CurAmtRange'
        Desc:
          maxLength: 100
          type: string
          description: Description that specifies the reason for the stop check.
        ExpDt:
          type: string
          description: "Date after which stop check is scheduled to expire.\n\n\n\nFormat: yyyy-mm-dd \n\nExample: 2021-11-20"
          format: date
        Name:
          maxLength: 80
          type: string
          description: Name of the payee.
        OrigDt:
          type: string
          description: "Date on which the customer originates the check.\n\n\n\nFormat: yyyy-mm-dd \n\nExample: 2021-11-20 "
          format: date
        StopChkDt:
          type: string
          description: "Date on which the stop check request is initiated.\n\n\n\nFormat: yyyy-mm-dd \n\nExample: 2021-11-20 "
          format: date
        StopChkReasonCode:
          type: string
          description: "Code to identify the reason for a stop check placed.\n\n**Note:** The following values are considered:\n\n- Lost\n\n- Stolen\n\n- Dispute\n\n- Fraud\n\n- CustomerRequest\n\n- BankRequest\n\n- Other "
          enum:
          - Lost
          - Stolen
          - Fraud
          - Dispute
          - BankRequest
          - CustomerRequest
          - Other
        TranFee:
          $ref: '#/components/schemas/stopCheckInqSecuredResponse_StopChkInfo_TranFee'
        VarianceAmt:
          $ref: '#/components/schemas/stopCheckInqSecuredResponse_StopChkInfo_VarianceAmt'
        VariancePct:
          type: number
          description: 'Variance percentage by which the amount of an item may vary from the amount specified in the check amount field and still be considered as a stop or suspect. '
      description: Information about the stop check.
    stopCheckInqSecuredResponse_StopChkKeys:
      required:
      - AcctKeys
      type: object
      properties:
        AcctKeys:
          $ref: '#/components/schemas/stopChkAddResponse_StopChkStatusRec_StopChkKeys_AcctKeys'
        StopChkId:
          maxLength: 36
          type: string
          description: |-
            Unique identification number to identify the stop check.

            **Note:** Refers to the native key.
        StopChkIdent:
          maxLength: 36
          type: string
          description: |-
            Unique identification code used in combination with account number and account type to identify the stop check record.

            **Note:** Composition of the key includes AcctKeys when KeyCompatiblity_Rule is "true" for the ClientAppName.
      description: |-
        Unique information to identify a stop check record.

        **Note:** Key may vary depending on the configuration. The key can be expressed as a native key (StopChkId) or as a compatibility key (AcctKeys and StopChkIdent).
    stopCheckInqSecuredResponse_StopChkStatus:
      required:
      - StopChkStatusCode
      type: object
      properties:
        EffDt:
          pattern: '[0-9]{4}-[0-9]{2}-[0-9]{2}(T[0-9]{2}:[0-9]{2}(:[0-9]{2}(\.[0-9]{3})?((-|\+)[0-9]{2}:[0-9]{2})?)?)?'
          type: string
          description: "Effective date and time when the stop check status is changed. \n\nFormat: yyyy-MM-ddTHH:mm:ss.SSS\n\nExample: 2021-01-31T23:59:59.999"
        StopChkStatusCode:
          type: string
          description: Code to indicate the status for the stop check record.
          enum:
          - Valid
      description: Details of the stop check status.
    stopCheckInqSecuredResponse_StopChkRec:
      required:
      - StopChkInfo
      - StopChkKeys
      - StopChkStatus
      properties:
        StopChkInfo:
          $ref: '#/components/schemas/stopCheckInqSecuredResponse_StopChkInfo'
        StopChkKeys:
          $ref: '#/components/schemas/stopCheckInqSecuredResponse_StopChkKeys'
        StopChkStatus:
          $ref: '#/components/schemas/stopCheckInqSecuredResponse_StopChkStatus'
    stopChkModRequest_StopChkKeys:
      required:
      - AcctKeys
      type: object
      properties:
        StopChkId:
          maxLength: 36
          type: string
          description: |+
            Unique identification number to identify the stop check.

            **Note:** Refers to the native key.

        AcctKeys:
          $ref: '#/components/schemas/stopChkCanSecuredRequest_StopChkKeys_AcctKeys'
        StopChkIdent:
          maxLength: 36
          type: string
          description: |-
            Unique identification code used in combination with account number and account type to identify the stop check record.

            **Note:** Client application uses StopChkIdent with a compatibility key.



            If StopChkId is provided in the selection request, then it will take precedence over any other selection criteria.
      description: |-
        Unique information to identify a stop check record.

        **Note:** Key may vary depending on the configuration. The key can be expressed as a native key (StopChkId) or as a compatibility key (AcctKeys and StopChkIdent).
    stopChkModRequest_StopChkInfo_TranFee_FeeAmt_CurCode:
      required:
      - CurCodeValue
      type: object
      properties:
        CurCodeType:
          type: string
          description: "Standard ISO currency code format. \n\n\n\nRefer to the <a href=\"https://en.wikipedia.org/wiki/ISO_4217\">ISO 4217 codes </a> to view the list of all currency codes."
          enum:
          - ISO4217-Alpha
        CurCodeValue:
          type: string
          description: "Currency code value as per the ISO currency code format set in the CurCodeType field.\n\nFor example, currency code for the US Dollar in alpha and numeric formats are:\n\n- USD (in ISO4217-Alpha format) \n\n- 840 (in ISO4217-Numeric format)\n\n\n\nPresently only USD currency code is supported.\n\n"
      description: |-
        Details of currency code.

        **Note:** This field cannot be modified.
    stopChkModRequest_StopChkInfo_TranFee_FeeAmt:
      type: object
      properties:
        Amt:
          type: number
          description: Amount in decimal format.
        CurCode:
          $ref: '#/components/schemas/stopChkModRequest_StopChkInfo_TranFee_FeeAmt_CurCode'
      description: |-
        Details of the stop check transaction processing fee amount.

        **Note:** This field cannot be modified.
    stopChkModRequest_StopChkInfo_TranFee_BillingInstruction_FromAcctKeys:
      required:
      - AcctId
      type: object
      properties:
        AcctId:
          maxLength: 36
          type: string
          description: |+
            Unique identification number of an account record.





        AcctType:
          type: string
          description: |-
            Type of an account.



            For more information about account types, refer to the Account Types section in <a href="../docs?path=docs/api-glossary.md"  title="Click to open"> Glossary</a>.
      description: |-
        Unique information to identify from account record.

        **Note:** This field cannot be modified.
    stopChkModRequest_StopChkInfo_TranFee_BillingInstruction:
      required:
      - BillingMethod
      type: object
      properties:
        BillingMethod:
          type: string
          description: |+
            Type of billing.

          enum:
          - DirectDebit
        FromAcctKeys:
          $ref: '#/components/schemas/stopChkModRequest_StopChkInfo_TranFee_BillingInstruction_FromAcctKeys'
      description: |-
        Details of the billing instruction used to specify how to charge for the stop check transaction fee.

        **Note:** This field cannot be modified.
    stopChkModRequest_StopChkInfo_TranFee:
      type: object
      properties:
        FeeIdent:
          maxLength: 36
          type: string
          description: Unique identification number to identify the fee amount.
        FeeAmt:
          $ref: '#/components/schemas/stopChkModRequest_StopChkInfo_TranFee_FeeAmt'
        BillingInstruction:
          $ref: '#/components/schemas/stopChkModRequest_StopChkInfo_TranFee_BillingInstruction'
      description: |-
        Information about billing details and transaction fee for a stop check transaction.

        **Note:** This field cannot be modified.
    stopChkModRequest_StopChkInfo:
      type: object
      properties:
        ChkNumRange:
          $ref: '#/components/schemas/stopChkAddRequest_StopChkInfo_ChkNumRange'
        Name:
          maxLength: 80
          type: string
          description: Name of the payee.
        CurAmt:
          $ref: '#/components/schemas/stopChkAddRequest_StopChkInfo_CurAmt'
        VariancePct:
          type: number
          description: "Variance percentage by which the amount of an item may vary from the amount specified in the check amount field and still be considered as a stop or suspect. \n\n**Note:** Request should not include variance percentage if the CurAmtRange describes an amount variance."
        CurAmtRange:
          $ref: '#/components/schemas/stopChkAddRequest_StopChkInfo_CurAmtRange'
        OrigDt:
          type: string
          description: "Date on which the customer originates the check.\n\n\n\nFormat: yyyy-mm-dd \n\nExample: 2021-11-20 "
          format: date
        StopChkDt:
          type: string
          description: "Date on which the stop check request is initiated.\n\n\n\nFormat: yyyy-mm-dd \n\nExample: 2021-11-20 "
          format: date
        ExpDt:
          type: string
          description: "Date after which stop check is scheduled to expire.\n\n\n\nFormat: yyyy-mm-dd \n\nExample: 2021-11-20"
          format: date
        StopChkReasonCode:
          type: string
          description: Code to identify the reason for a stop check placed.
          enum:
          - Lost
          - Stolen
          - Dispute
          - Fraud
          - CustomerRequest
          - BankRequest
          - Other
        Desc:
          maxLength: 100
          type: string
          description: Description that specifies the reason for the stop check.
        TranFee:
          $ref: '#/components/schemas/stopChkModRequest_StopChkInfo_TranFee'
        CardSignedInd:
          type: boolean
          description: Flag to indicate the customer has signed a stop payment card.
      description: Information about the client-managed properties of the stop check record.
