name: Webhook Event Handling

on:
  push:
    branches:
      - main
  status:
    types:
      - failure

jobs:
  handle-webhook:
    runs-on: ubuntu-latest

    steps:
      - name: Check Webhook Payload
        id: check-payload
        run: | 
          echo "Webhook Payload: ${{ toJson(github.event) }}"

      - name: Handle Failed Webhook
        if: github.event_name == 'status' && github.event.state == 'failure'
        run: |
          # Perform actions based on the failed webhook event
          echo "Webhook Payload failed"
